apiVersion: isovalent.com/v1alpha1
kind: LBVIP
metadata:
  namespace: default
  name: vip-1
spec:
  ipv4Request: "100.64.0.100"
#
# Disable this case for now because this dynamic allocation
# may conflict with the current LBFrontend implementation.
# The T1 Services may request an IP allocated by this LBVIP,
# but with a different sharing-key.
#
# ---
# apiVersion: isovalent.com/v1alpha1
# kind: LBVIP
# metadata:
#   namespace: default
#   name: vip-2
# spec: {}
