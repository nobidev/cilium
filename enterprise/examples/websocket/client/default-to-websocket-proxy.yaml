apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  namespace: default
  name: default-to-websocket-proxy
spec:
  endpointSelector:
    matchExpressions:
    - {key: app, operator: NotIn, values: [http-proxy]}
  egress:
  - toEntities:
    - world
    toPorts:
    - ports:
      - port: "443"
        protocol: TCP
      - port: "22"
        protocol: TCP
      listener:
        envoyConfig:
          name: websocket-proxy-encap
          kind: CiliumClusterwideEnvoyConfig
        name: cilium-websocket-encap
  - toPorts:
    - ports:
      - port: "53"
        protocol: ANY
      rules:
        dns:
        - matchPattern: "*"
