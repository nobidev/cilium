apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: clife
    app.kubernetes.io/managed-by: kustomize
  name: metrics
  namespace: cilium
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 18443
  selector:
    control-plane: controller-manager
