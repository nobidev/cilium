{{- /*Auto-generated by features/helm-gen. Do not edit by hand.*/ -}}
{{- define "enterprise.validateFeatures" }}{{- $featureMaturityLevels := dict "Alpha" 3 "Beta" 2 "Limited" 1 "Stable" 0  }}
{{- $validFeatureGates := list "BFD" "BGPControlPlane" "CIDRMatchModePolicies" "CNIChainingMode" "CiliumEndpointSlice" "ClusterMesh" "ClusterMeshKVStoreMesh" "ClusterMeshMixedRoutingMode" "ClusterMeshOverlappingPodCIDR" "ClusterMeshPhantomServices" "DNSProxyHA" "DatapathBandwidthManager" "DatapathHostFirewall" "DatapathIPModeDualStack" "DatapathIPModeIPv4" "DatapathIPModeIPv6" "DatapathModeLBOnly" "DatapathModeNetkit" "DatapathModeVETH" "DatapathTunnelModeGeneve" "DatapathTunnelModeVXLAN" "EgressGatewayHA" "EgressGatewayIPv4" "EncryptionIPSec" "EncryptionPolicy" "EncryptionWireguard" "EncryptionWireguardNodeToNode" "EnterpriseBGPControlPlane" "EnvoyDaemonSet" "ExternalWorkloads" "GatewayAPI" "IPAMAzure" "IPAMClusterPool" "IPAMENI" "IPAMMultiPool" "IdentityCRD" "IdentityKVStore" "IngressController" "L2Announcement" "L7Proxy" "LocalRedirectPolicy" "MultiNetwork" "Multicast" "MutualAuthSpire" "NodeIPAMLoadBalancer" "NonDefaultDenyPolicies" "SCTP" "SRv6" "VTEPIntegration"  }}
{{- range .enterprise.featureGate.approved -}}
{{- if not (has . $validFeatureGates) -}}
  {{- fail (printf "Invalid feature gate exception %q" .) }}
{{- end -}}
{{- end -}}
{{- $minimumMaturityOrder := 0 -}}
{{- $strictFeatureGates := .enterprise.featureGate.strict -}}
{{- $fails := false -}}
{{- if (eq "true" (print .enterprise.bfd.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "BFD" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: BFD was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature BFD was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .bgpControlPlane.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "BGPControlPlane" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: BGPControlPlane was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature BGPControlPlane was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "nodes" (print .policyCIDRMatchMode))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "CIDRMatchModePolicies" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: CIDRMatchModePolicies was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature CIDRMatchModePolicies was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if not (or (kindIs "invalid" .cni.chainingMode) (eq .cni.chainingMode "none")) -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "CNIChainingMode" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: CNIChainingMode was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature CNIChainingMode was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .ciliumEndpointSlice.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "CiliumEndpointSlice" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: CiliumEndpointSlice was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature CiliumEndpointSlice was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (and (eq "true" (print .clustermesh.apiserver.kvstoremesh.enabled)) (eq "true" (print .clustermesh.useAPIServer)) ) -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "ClusterMeshKVStoreMesh" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: ClusterMeshKVStoreMesh was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature ClusterMeshKVStoreMesh was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .enterprise.clustermesh.mixedRoutingMode.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "ClusterMeshMixedRoutingMode" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: ClusterMeshMixedRoutingMode was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature ClusterMeshMixedRoutingMode was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .enterprise.clustermesh.enableOverlappingPodCIDRSupport))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "ClusterMeshOverlappingPodCIDR" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: ClusterMeshOverlappingPodCIDR was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature ClusterMeshOverlappingPodCIDR was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .enterprise.clustermesh.phantomServices.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "ClusterMeshPhantomServices" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: ClusterMeshPhantomServices was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature ClusterMeshPhantomServices was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if get .extraConfig "external-dns-proxy" -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "DNSProxyHA" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: DNSProxyHA was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature DNSProxyHA was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .bandwidthManager.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "DatapathBandwidthManager" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: DatapathBandwidthManager was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature DatapathBandwidthManager was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .hostFirewall.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "DatapathHostFirewall" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: DatapathHostFirewall was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature DatapathHostFirewall was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (and (eq "true" (print .ipv4.enabled)) (eq "true" (print .ipv6.enabled)) ) -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "DatapathIPModeDualStack" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: DatapathIPModeDualStack was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature DatapathIPModeDualStack was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (and (eq "false" (print .ipv4.enabled)) (eq "true" (print .ipv6.enabled)) ) -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "DatapathIPModeIPv6" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: DatapathIPModeIPv6 was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature DatapathIPModeIPv6 was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "netkit" (print .bpf.datapathMode))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "DatapathModeNetkit" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: DatapathModeNetkit was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature DatapathModeNetkit was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .enterprise.egressGatewayHA.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "EgressGatewayHA" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: EgressGatewayHA was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature EgressGatewayHA was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .egressGateway.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "EgressGatewayIPv4" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: EgressGatewayIPv4 was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature EgressGatewayIPv4 was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (and (eq "true" (print .encryption.enabled)) (eq "ipsec" (print .encryption.type)) ) -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "EncryptionIPSec" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: EncryptionIPSec was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature EncryptionIPSec was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (and (eq "true" (print .enterprise.encryption.policy.enabled)) (eq "plaintext" (print .enterprise.encryption.policy.fallbackBehavior)) ) -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "EncryptionPolicy" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: EncryptionPolicy was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature EncryptionPolicy was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (and (eq "true" (print .encryption.enabled)) (eq "wireguard" (print .encryption.type)) ) -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "EncryptionWireguard" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: EncryptionWireguard was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature EncryptionWireguard was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (and (eq "true" (print .encryption.nodeEncryption)) (eq "wireguard" (print .encryption.type)) ) -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "EncryptionWireguardNodeToNode" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: EncryptionWireguardNodeToNode was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature EncryptionWireguardNodeToNode was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .enterprise.bgpControlPlane.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "EnterpriseBGPControlPlane" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: EnterpriseBGPControlPlane was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature EnterpriseBGPControlPlane was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .envoy.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "EnvoyDaemonSet" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: EnvoyDaemonSet was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature EnvoyDaemonSet was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .externalWorkloads.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "ExternalWorkloads" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: ExternalWorkloads was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature ExternalWorkloads was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .gatewayAPI.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "GatewayAPI" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: GatewayAPI was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature GatewayAPI was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "azure" (print .ipam.mode))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "IPAMAzure" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: IPAMAzure was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature IPAMAzure was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "multi-pool" (print .ipam.mode))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "IPAMMultiPool" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: IPAMMultiPool was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature IPAMMultiPool was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .ingressController.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "IngressController" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: IngressController was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature IngressController was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .l2announcements.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "L2Announcement" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: L2Announcement was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature L2Announcement was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .localRedirectPolicy))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "LocalRedirectPolicy" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: LocalRedirectPolicy was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature LocalRedirectPolicy was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .enterprise.multiNetwork.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "MultiNetwork" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: MultiNetwork was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature MultiNetwork was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .enterprise.multicast.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "Multicast" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: Multicast was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature Multicast was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .authentication.mutual.spire.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "MutualAuthSpire" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: MutualAuthSpire was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature MutualAuthSpire was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .nodeIPAM.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "NodeIPAMLoadBalancer" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: NodeIPAMLoadBalancer was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature NodeIPAMLoadBalancer was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .enableNonDefaultDenyPolicies))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Limited") $minimumMaturityOrder) (not (has "NonDefaultDenyPolicies" .enterprise.featureGate.approved))) -}}
      {{- fail "\nLimited feature: NonDefaultDenyPolicies was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Limited feature NonDefaultDenyPolicies was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .sctp.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "SCTP" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: SCTP was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature SCTP was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .enterprise.srv6.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "SRv6" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: SRv6 was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature SRv6 was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}
{{- if (eq "true" (print .vtep.enabled))  -}}
  {{- if $strictFeatureGates -}}
    {{- if (and (gt (get $featureMaturityLevels "Beta") $minimumMaturityOrder) (not (has "VTEPIntegration" .enterprise.featureGate.approved))) -}}
      {{- fail "\nBeta feature: VTEPIntegration was enabled, but is not an approved feature. Please contact Isovalent Support for more information on how to grant an exception." -}}
    {{- end -}}
  {{- else -}}
    {{ $fails = true }}
WARNING: A Beta feature VTEPIntegration was enabled, but it is not an approved feature.
  {{- end -}}
{{- end -}}{{- if $fails }}
Please contact Isovalent Support for more information on how to grant an exception.
{{- end }}
{{- end }}
