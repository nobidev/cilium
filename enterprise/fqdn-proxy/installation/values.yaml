# File generated by enterprise/fqdn-proxy/installation/Makefile; DO NOT EDIT.
# This file is based on enterprise/fqdn-proxy/installation/cilium/values.yaml.tmpl.

# Default values for dnsproxy.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  override: ~
  repository: "quay.io/isovalent-dev/cilium-dnsproxy-ci"
  tag: "latest"
  imagePullPolicy: "Always"
runPath: "/var/run/cilium"
gke: false
debug: false
pprof:
  enabled: false
offlineMode:
  enabled: false
extraArgs: []
extraVolumes: []
extraVolumeMounts: []
podSecurityContext: {}
# -- Labels to be added to cilium-dnsproxy pods
podLabels: {}
# -- Explicitly enable or disable priority class.
# .Capabilities.KubeVersion is unsettable in `helm template` calls,
# it depends on k8s libraries version that Helm was compiled against.
# This option allows to explicitly disable setting the priority class, which
# is useful for rendering charts for
enableCriticalPriorityClass: false
# -- The priority class to use for cilium-dnsproxy pods
priorityClassName: ""
resources: {}
# -- The pod security context, by default adds NET_ADMIN, NET_RAW and BPF (if needed).
securityContext: {}
metrics:
  # -- Enable Prometheus metrics.
  enabled: true
  # -- Prometheus metrics port.
  port: 9967
  serviceMonitor:
    # -- Enable service monitors.
    # This requires the prometheus CRDs to be available (see https://github.com/prometheus-operator/prometheus-operator/blob/master/example/prometheus-operator-crd/monitoring.coreos.com_servicemonitors.yaml)
    enabled: false
    # -- Labels to add to cilium-dnsproxy ServiceMonitor
    labels: {}
    # -- Annotations to add to cilium-dnsproxy ServiceMonitor
    annotations: {}
    # -- Scrape interval.
    scrapeInterval: 10s
serviceAccount:
  create: true
  automount: true
  name: cilium-dnsproxy
  annotations: {}
# -- Node tolerations for proxy scheduling to nodes with taints
# ref: https://kubernetes.io/docs/concepts/configuration/assign-pod-node/
tolerations:
- operator: Exists
  # - key: "key"
  #   operator: "Equal|Exists"
  #   value: "value"
  #   effect: "NoSchedule|PreferNoSchedule|NoExecute(1.6 only)"

# -- Affinity for cilium-dnsproxy.
affinity:
  podAntiAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
    - topologyKey: kubernetes.io/hostname
      labelSelector:
        matchLabels:
          k8s-app: cilium-dnsproxy

# Ref: https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/#pod-s-dns-policy
dnsPolicy: ""

# -- Node selector for cilium-dnsproxy.
nodeSelector:
  kubernetes.io/os: linux

# -- Annotations to add to cilium-dnsproxy DaemonSet
annotations: {}
