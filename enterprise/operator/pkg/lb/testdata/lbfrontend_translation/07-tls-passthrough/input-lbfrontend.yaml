apiVersion: isovalent.com/v1alpha1
kind: LBFrontend
metadata:
  namespace: default
  name: lb-1
spec:
  vip: 100.64.0.100
  port: 80
  applications:
    tlsPassthrough:
      routes:
        - hostNames:
            - "foo.acme.io"
          backendRef:
            name: lb-1
        - hostNames:
            - "bar.acme.io"
          backendRef:
            name: lb-1
