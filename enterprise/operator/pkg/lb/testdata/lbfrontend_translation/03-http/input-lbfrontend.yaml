apiVersion: isovalent.com/v1alpha1
kind: LBFrontend
metadata:
  namespace: default
  name: lb-1
spec:
  vipRef:
    name: lb-1
  port: 80
  applications:
    httpProxy:
      routes:
        - backendRef:
            name: lb-1
        - hostNames:
            - "bar.acme.io"
          path:
            exact: "/api"
          backendRef:
            name: lb-2
