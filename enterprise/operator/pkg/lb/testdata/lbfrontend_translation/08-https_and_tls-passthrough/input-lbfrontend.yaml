apiVersion: isovalent.com/v1alpha1
kind: LBFrontend
metadata:
  namespace: default
  name: lb-1
spec:
  vip: 100.64.0.100
  port: 80
  applications:
    httpsProxy:
      tlsConfig:
        certificates:
          - secretName: "test"
      routes:
        - hostNames:
            - "bar.acme.io"
          backendRef:
            name: lb-1
    tlsPassthrough:
      routes:
        - hostNames:
            - "foo.acme.io"
          backendRef:
            name: lb-1
