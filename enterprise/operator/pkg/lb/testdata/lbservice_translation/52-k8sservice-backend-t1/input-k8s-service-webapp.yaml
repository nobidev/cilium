apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: webapp
spec:
  ports:
    - name: web
      port: 8080
      protocol: TCP
      targetPort: web
  selector:
    name: webapp
  sessionAffinity: None
  type: ClusterIP
