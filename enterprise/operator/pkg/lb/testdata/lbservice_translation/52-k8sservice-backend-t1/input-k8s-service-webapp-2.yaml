apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: webapp-2
spec:
  ports:
    - name: web
      port: 8080
      protocol: TCP
      targetPort: web
  selector:
    name: webapp-2
  sessionAffinity: None
  type: ClusterIP
