apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: webapp3
spec:
  ports:
    - name: web
      port: 8080
      protocol: TCP
      targetPort: web
  selector:
    name: webapp3
  sessionAffinity: None
  type: ClusterIP
