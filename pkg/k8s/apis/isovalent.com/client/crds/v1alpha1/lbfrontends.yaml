---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: lbfrontends.isovalent.com
spec:
  group: isovalent.com
  names:
    categories:
    - cilium
    - isovalent
    - loadbalancer
    kind: LBFrontend
    listKind: LBFrontendList
    plural: lbfrontends
    shortNames:
    - lbfe
    singular: lbfrontend
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.vipRef.name
      name: VIP Reference
      type: string
    - jsonPath: .status.addresses.ipv4
      name: VIP IPv4
      type: string
    - jsonPath: .spec.port
      name: Port
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec is a spec .
            properties:
              applications:
                properties:
                  httpProxy:
                    properties:
                      httpConfig:
                        properties:
                          enableHTTP2:
                            type: boolean
                          enableHTTP11:
                            type: boolean
                        type: object
                        x-kubernetes-validations:
                        - message: At least one http version must be enabled
                          rule: (has(self.enableHTTP11) && self.enableHTTP11) || (has(self.enableHTTP2)
                            && self.enableHTTP2)
                      routes:
                        items:
                          properties:
                            backendRef:
                              properties:
                                name:
                                  minLength: 1
                                  type: string
                              required:
                              - name
                              type: object
                            match:
                              properties:
                                hostNames:
                                  items:
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                    type: string
                                  type: array
                                path:
                                  properties:
                                    exact:
                                      minLength: 1
                                      type: string
                                    prefix:
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: Exactly one path type (exact or prefix)
                                      must be specified
                                    rule: (has(self.exact) || has(self.prefix)) &&
                                      !(has(self.exact) && has(self.prefix))
                              type: object
                          required:
                          - backendRef
                          type: object
                        minItems: 1
                        type: array
                    required:
                    - routes
                    type: object
                  httpsProxy:
                    properties:
                      httpConfig:
                        properties:
                          enableHTTP2:
                            type: boolean
                          enableHTTP11:
                            type: boolean
                        type: object
                        x-kubernetes-validations:
                        - message: At least one http version must be enabled
                          rule: (has(self.enableHTTP11) && self.enableHTTP11) || (has(self.enableHTTP2)
                            && self.enableHTTP2)
                      routes:
                        items:
                          properties:
                            backendRef:
                              properties:
                                name:
                                  minLength: 1
                                  type: string
                              required:
                              - name
                              type: object
                            match:
                              properties:
                                hostNames:
                                  items:
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                    type: string
                                  type: array
                                path:
                                  properties:
                                    exact:
                                      minLength: 1
                                      type: string
                                    prefix:
                                      minLength: 1
                                      type: string
                                  type: object
                                  x-kubernetes-validations:
                                  - message: Exactly one path type (exact or prefix)
                                      must be specified
                                    rule: (has(self.exact) || has(self.prefix)) &&
                                      !(has(self.exact) && has(self.prefix))
                              type: object
                          required:
                          - backendRef
                          type: object
                        minItems: 1
                        type: array
                      tlsConfig:
                        properties:
                          certificates:
                            items:
                              properties:
                                secretName:
                                  minLength: 1
                                  type: string
                              required:
                              - secretName
                              type: object
                            minItems: 1
                            type: array
                        required:
                        - certificates
                        type: object
                    required:
                    - routes
                    type: object
                  tlsPassthrough:
                    properties:
                      routes:
                        items:
                          properties:
                            backendRef:
                              properties:
                                name:
                                  minLength: 1
                                  type: string
                              required:
                              - name
                              type: object
                            match:
                              properties:
                                hostNames:
                                  items:
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                    type: string
                                  type: array
                              type: object
                          required:
                          - backendRef
                          type: object
                        minItems: 1
                        type: array
                    required:
                    - routes
                    type: object
                type: object
                x-kubernetes-validations:
                - message: Exactly one application (httpProxy, httpsProxy or tlsPassthrough)
                    must be specified
                  rule: (has(self.httpProxy) || has(self.httpsProxy) || has(self.tlsPassthrough))
                    && !(has(self.httpProxy) && has(self.httpsProxy)) && !(has(self.httpProxy)
                    && has(self.tlsPassthrough)) && !(has(self.httpsProxy) && has(self.tlsPassthrough))
              port:
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              vipRef:
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - applications
            - port
            - vipRef
            type: object
          status:
            description: Status is a status .
            properties:
              addresses:
                description: |-
                  Allocated addresses copied from the LBVIP status. This is just for
                  displaying the allocated addresses in the kubectl get output.
                properties:
                  ipv4:
                    type: string
                type: object
              conditions:
                description: Conditions describe the current conditions of the LBFrontend.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            required:
            - addresses
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
