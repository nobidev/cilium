---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: isovalentlbs.isovalent.com
spec:
  group: isovalent.com
  names:
    categories:
    - cilium
    - isovalent
    kind: IsovalentLB
    listKind: IsovalentLBList
    plural: isovalentlbs
    shortNames:
    - ilb
    singular: isovalentlb
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec is a spec .
            properties:
              backends:
                items:
                  properties:
                    ip:
                      format: ip
                      type: string
                    port:
                      format: int32
                      type: integer
                  required:
                  - ip
                  - port
                  type: object
                type: array
              healthcheck:
                properties:
                  interval:
                    format: duration
                    type: string
                required:
                - interval
                type: object
              port:
                format: int32
                type: integer
              vip:
                type: string
            required:
            - backends
            - healthcheck
            - port
            - vip
            type: object
          status:
            description: Status is a status .
            properties:
              bar:
                type: string
            required:
            - bar
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
