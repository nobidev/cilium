---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: lbbackends.isovalent.com
spec:
  group: isovalent.com
  names:
    categories:
    - cilium
    - isovalent
    - loadbalancer
    kind: LBBackend
    listKind: LBBackendList
    plural: lbbackends
    shortNames:
    - lbbe
    singular: lbbackend
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec is a spec .
            properties:
              addresses:
                items:
                  properties:
                    ip:
                      format: ip
                      type: string
                    port:
                      format: int32
                      type: integer
                  required:
                  - ip
                  - port
                  type: object
                type: array
              healthCheck:
                properties:
                  healthyThreshold:
                    default: 2
                    format: int32
                    minimum: 1
                    type: integer
                  http:
                    properties:
                      host:
                        default: lb
                        minLength: 1
                        type: string
                      path:
                        default: /healthz
                        minLength: 1
                        type: string
                    type: object
                  intervalSeconds:
                    default: 30
                    format: int32
                    minimum: 1
                    type: integer
                  tcp:
                    type: object
                  timeoutSeconds:
                    default: 5
                    format: int32
                    minimum: 1
                    type: integer
                  unhealthyThreshold:
                    default: 2
                    format: int32
                    minimum: 1
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: Exactly one health check (HTTP or TCP) must be specified
                  rule: (has(self.tcp) || has(self.http)) && !(has(self.tcp) && has(self.http))
            required:
            - addresses
            - healthCheck
            type: object
          status:
            description: Status is a status .
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
