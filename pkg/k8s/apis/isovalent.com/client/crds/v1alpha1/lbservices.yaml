---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: lbservices.isovalent.com
spec:
  group: isovalent.com
  names:
    categories:
    - cilium
    - isovalent
    - loadbalancer
    kind: LBService
    listKind: LBServiceList
    plural: lbservices
    shortNames:
    - lbsvc
    singular: lbservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.vipRef.name
      name: VIP Reference
      type: string
    - jsonPath: .status.addresses.ipv4
      name: VIP IPv4
      type: string
    - jsonPath: .status.addresses.ipv6
      name: VIP IPv6
      type: string
    - jsonPath: .spec.port
      name: Port
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          LBService defines a Service frontend of the Isovalent Load Balancer.
          It's assigned to a LBVIP and and load balances traffic to one or multiple
          LBBackendPool's.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              applications:
                description: |-
                  The configuration for the applications that running on the port.
                  While the name is plural, only one application can be specified
                  currently.
                oneOf:
                - properties:
                    httpProxy: {}
                  required:
                  - httpProxy
                - properties:
                    httpsProxy: {}
                  required:
                  - httpsProxy
                - properties:
                    tlsPassthrough: {}
                  required:
                  - tlsPassthrough
                - properties:
                    tlsProxy: {}
                  required:
                  - tlsProxy
                - properties:
                    tcpProxy: {}
                  required:
                  - tcpProxy
                - properties:
                    udpProxy: {}
                  required:
                  - udpProxy
                properties:
                  httpProxy:
                    description: |-
                      Defining this stanza enables HTTPProxy application that proxies the
                      HTTP traffic to the backends over TCP connection.
                    properties:
                      auth:
                        description: Optional HTTP authN/authZ configuration for the
                          HTTP proxy application.
                        oneOf:
                        - properties:
                            basic: {}
                          required:
                          - basic
                        - properties:
                            jwt: {}
                          required:
                          - jwt
                        properties:
                          basic:
                            description: The basic authentication configuration.
                            properties:
                              users:
                                description: Users for the basic authentication.
                                properties:
                                  secretRef:
                                    description: |-
                                      The reference to the k8s secret that contains the username and
                                      password for the basic authentication. This must be a k8s secret of
                                      type Opaque with the username as a key and the password as a value.
                                      A single secret can contain multiple username-password pairs.
                                    properties:
                                      name:
                                        description: The name of the K8s Secret resource.
                                        minLength: 1
                                        type: string
                                    required:
                                    - name
                                    type: object
                                required:
                                - secretRef
                                type: object
                            required:
                            - users
                            type: object
                          jwt:
                            description: The jwt authentication configuration.
                            properties:
                              providers:
                                description: |-
                                  The list of JWT authentication providers. At least one provider must
                                  be specified.
                                items:
                                  properties:
                                    audiences:
                                      description: |-
                                        List of expected audiences. If specified, one of the audience has to
                                        match the aud claim in the JWT. Otherwise, the aud field is not
                                        checked.
                                      items:
                                        type: string
                                      maxItems: 8
                                      type: array
                                    issuer:
                                      description: |-
                                        An expected issuer (iss) of the JWT. If specified, it has to match
                                        the iss claim in the JWT. Otherwise, the iss field is not checked.
                                      minLength: 1
                                      type: string
                                    jwks:
                                      description: The JWT authentication configuration.
                                      properties:
                                        httpURI:
                                          description: Get JWKS from remote server
                                            with HTTP.
                                          properties:
                                            uri:
                                              description: The remote HTTP URI. The
                                                scheme must be http or https.
                                              format: uri
                                              type: string
                                              x-kubernetes-validations:
                                              - message: The scheme must be http or
                                                  https
                                                rule: self.startsWith('http') || self.startsWith('https')
                                          required:
                                          - uri
                                          type: object
                                        secretRef:
                                          description: |-
                                            The reference to the k8s Secret contains JWKS. The Secret must be an
                                            Opaque Secret with "jwks" key and base64-encoded JWKS string as a
                                            value.
                                          properties:
                                            name:
                                              description: The name of the K8s Secret
                                                resource.
                                              minLength: 1
                                              type: string
                                          required:
                                          - name
                                          type: object
                                      type: object
                                      x-kubernetes-validations:
                                      - message: Either secretRef or httpURI must
                                          be specified
                                        rule: (has(self.secretRef)?1:0)+(has(self.httpURI)?1:0)==1
                                    name:
                                      description: The name of the provider.
                                      type: string
                                  required:
                                  - jwks
                                  - name
                                  type: object
                                maxItems: 8
                                minItems: 1
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                            required:
                            - providers
                            type: object
                        type: object
                      connectionFiltering:
                        description: |-
                          The optional connection filtering configuration for all HTTP connections.
                          It defines the connection attributes that should be obtained to decide
                          whether connections should be denied or allowed.
                        properties:
                          ruleType:
                            description: The type of the rules.
                            enum:
                            - Allow
                            - Deny
                            type: string
                          rules:
                            description: |-
                              Configure the rules that should be used for the HTTP application.
                              Each rule needs to define at least one property to filter on.
                              The properties of each entry are logically ANDed.
                            items:
                              anyOf:
                              - properties:
                                  sourceCIDR: {}
                                required:
                                - sourceCIDR
                              properties:
                                sourceCIDR:
                                  description: Source CIDR based matching. This allows
                                    for matching a specific or a range of IPv4 or
                                    IPv6 addresses.
                                  properties:
                                    cidr:
                                      description: |-
                                        CIDR of the source.
                                        This must be in CIDR notation and use a /32 to express
                                        a single host.
                                      format: cidr
                                      minLength: 1
                                      type: string
                                  required:
                                  - cidr
                                  type: object
                              type: object
                            minItems: 1
                            type: array
                        required:
                        - ruleType
                        type: object
                      httpConfig:
                        anyOf:
                        - properties:
                            enableHTTP11: {}
                          required:
                          - enableHTTP11
                        - properties:
                            enableHTTP2: {}
                          required:
                          - enableHTTP2
                        description: The application-wide HTTP configuration.
                        properties:
                          enableHTTP2:
                            description: Setting this to true enables HTTP2.
                            type: boolean
                          enableHTTP11:
                            description: Setting this to true enables HTTP/1.1.
                            type: boolean
                        type: object
                      rateLimits:
                        anyOf:
                        - properties:
                            connections: {}
                          required:
                          - connections
                        description: |-
                          Optional global rate limit configuration for the HTTP proxy application.
                          Currently, this is only a local rate limit (enforced on each LB node individually).
                        properties:
                          connections:
                            description: |-
                              Rate limiting on connection basis.
                              It is applied and enforced when the connection is established.
                            properties:
                              limit:
                                minimum: 1
                                type: integer
                              timePeriodSeconds:
                                minimum: 1
                                type: integer
                            required:
                            - limit
                            - timePeriodSeconds
                            type: object
                        type: object
                      routes:
                        description: The HTTP routing configuration.
                        items:
                          properties:
                            auth:
                              description: Optional per-route authN/authZ configuration.
                              properties:
                                basic:
                                  description: The per-route basic authentication
                                    configuration.
                                  properties:
                                    disabled:
                                      description: Disables the basic authentication
                                        for this route.
                                      type: boolean
                                  required:
                                  - disabled
                                  type: object
                                jwt:
                                  description: The per-route JWT authentication configuration.
                                  properties:
                                    disabled:
                                      description: Disables the JWT authentication
                                        for this route.
                                      type: boolean
                                  required:
                                  - disabled
                                  type: object
                              type: object
                            backendRef:
                              description: |-
                                The reference to the LBBackendPool resource that this route should
                                forward the traffic to when the route is matched. The referred
                                LBBackendPool must exist in the same namespace as the LBService.
                              properties:
                                name:
                                  description: The name of the LBBackendPool resource.
                                  minLength: 1
                                  type: string
                              required:
                              - name
                              type: object
                            match:
                              description: |-
                                The HTTP route matching criteria. All conditions must be satisfied
                                for the route to be matched.
                              properties:
                                hostNames:
                                  description: |-
                                    The list of host names that the route should match. The host name is
                                    the value of the Host header in the HTTP request for plain-text
                                    HTTP. When TLS is enabled, the host name must match both the SNI and
                                    the Host header. The following formats are supported:

                                    - Exact domain names: www.example.com
                                    - Suffix domain wildcards: *.example.com
                                    - Special wildcard: * matching any domain

                                    Omitting this field is identical to specifying a wildcard "*".
                                  items:
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                    type: string
                                  type: array
                                path:
                                  description: |-
                                    The path matching criteria. When omitted, the route matches all
                                    paths.
                                  oneOf:
                                  - properties:
                                      exact: {}
                                    required:
                                    - exact
                                  - properties:
                                      prefix: {}
                                    required:
                                    - prefix
                                  properties:
                                    exact:
                                      description: Exact matching. The path must be
                                        exactly the same as the value.
                                      minLength: 1
                                      type: string
                                    prefix:
                                      description: Prefix matching. The path must
                                        start with the value.
                                      minLength: 1
                                      type: string
                                  type: object
                              type: object
                            persistentBackend:
                              anyOf:
                              - properties:
                                  sourceIP: {}
                                required:
                                - sourceIP
                              - properties:
                                  cookies: {}
                                required:
                                - cookies
                              - properties:
                                  headers: {}
                                required:
                                - headers
                              description: |-
                                The optional persistent backend configuration for this HTTP route.
                                It defines the request attributes that should be obtained to decide
                                whether requests should be sent to persistently the same backend.
                                The attributes are logically ANDed.

                                Note: Persistent backend configuration is only supported by LBBackendPools
                                with loadbalancing algorithm `consistentHashing`.
                              properties:
                                cookies:
                                  description: List of cookies for which requests
                                    are sent to the same backend if they match.
                                  items:
                                    properties:
                                      name:
                                        description: The name of the cookie that will
                                          be used to obtain the hash key.
                                        type: string
                                    required:
                                    - name
                                    type: object
                                  type: array
                                headers:
                                  description: List of headers for which requests
                                    are sent to the same backend if they match.
                                  items:
                                    properties:
                                      name:
                                        description: The name of the header that will
                                          be used to obtain the hash key.
                                        type: string
                                    required:
                                    - name
                                    type: object
                                  type: array
                                sourceIP:
                                  description: |-
                                    Whether requests from the same source IP should be sent to
                                    the same backend.
                                  type: boolean
                              type: object
                            rateLimits:
                              anyOf:
                              - properties:
                                  requests: {}
                                required:
                                - requests
                              description: |-
                                Optional per-route rate limit configuration.
                                Currently, this is only a local rate limit (enforced on each LB node individually).
                              properties:
                                requests:
                                  description: |-
                                    Configure max allowed requests for the HTTP route.
                                    It is applied and enforced before routing the HTTP request.
                                  properties:
                                    limit:
                                      minimum: 1
                                      type: integer
                                    timePeriodSeconds:
                                      minimum: 1
                                      type: integer
                                  required:
                                  - limit
                                  - timePeriodSeconds
                                  type: object
                              type: object
                            requestFiltering:
                              description: |-
                                The optional request filtering configuration for this HTTP route.
                                It defines the request attributes that should be obtained to decide
                                whether requests should be denied or allowed.
                              properties:
                                ruleType:
                                  description: The type of the rules.
                                  enum:
                                  - Allow
                                  - Deny
                                  type: string
                                rules:
                                  description: |-
                                    Configure the rules that should be used for the HTTP route.
                                    Each rule needs to define at least one property to filter on.
                                    The properties of each entry are logically ANDed.
                                  items:
                                    anyOf:
                                    - properties:
                                        sourceCIDR: {}
                                      required:
                                      - sourceCIDR
                                    - properties:
                                        hostName: {}
                                      required:
                                      - hostName
                                    - properties:
                                        path: {}
                                      required:
                                      - path
                                    - properties:
                                        headers: {}
                                      required:
                                      - headers
                                    - properties:
                                        jwtClaims: {}
                                      required:
                                      - jwtClaims
                                    properties:
                                      headers:
                                        description: Header-based matching. Only one
                                          of exact, suffix or regex match can be specified.
                                        items:
                                          properties:
                                            name:
                                              description: Name of the header.
                                              minLength: 1
                                              type: string
                                            value:
                                              description: Value of the header.
                                              oneOf:
                                              - properties:
                                                  exact: {}
                                                required:
                                                - exact
                                              - properties:
                                                  prefix: {}
                                                required:
                                                - prefix
                                              - properties:
                                                  regex: {}
                                                required:
                                                - regex
                                              properties:
                                                exact:
                                                  description: Exact matching. The
                                                    value must be exactly the same
                                                    as the value.
                                                  minLength: 1
                                                  type: string
                                                prefix:
                                                  description: Prefix matching. The
                                                    value must start with the value.
                                                  minLength: 1
                                                  type: string
                                                regex:
                                                  description: Regex matching. The
                                                    value must match the regex value.
                                                  minLength: 1
                                                  type: string
                                              type: object
                                          required:
                                          - name
                                          - value
                                          type: object
                                        type: array
                                      hostName:
                                        description: Host-based matching. Only one
                                          of exact or suffix match can be specified.
                                        oneOf:
                                        - properties:
                                            exact: {}
                                          required:
                                          - exact
                                        - properties:
                                            suffix: {}
                                          required:
                                          - suffix
                                        properties:
                                          exact:
                                            description: Exact matching. The hostname
                                              must be exactly the same as the value.
                                            minLength: 1
                                            type: string
                                          suffix:
                                            description: Suffix matching. The hostname
                                              must end with the value.
                                            minLength: 1
                                            type: string
                                        type: object
                                      jwtClaims:
                                        description: JWT claim based matching. Only
                                          one of exact, suffix or regex match can
                                          be specified.
                                        items:
                                          properties:
                                            name:
                                              description: Name of the JWT claim.
                                              minLength: 1
                                              type: string
                                            value:
                                              description: Value of the JWT claim.
                                              oneOf:
                                              - properties:
                                                  exact: {}
                                                required:
                                                - exact
                                              - properties:
                                                  prefix: {}
                                                required:
                                                - prefix
                                              - properties:
                                                  regex: {}
                                                required:
                                                - regex
                                              properties:
                                                exact:
                                                  description: Exact matching. The
                                                    value must be exactly the same
                                                    as the value.
                                                  minLength: 1
                                                  type: string
                                                prefix:
                                                  description: Prefix matching. The
                                                    value must start with the value.
                                                  minLength: 1
                                                  type: string
                                                regex:
                                                  description: Regex matching. The
                                                    value must match the regex value.
                                                  minLength: 1
                                                  type: string
                                              type: object
                                          required:
                                          - name
                                          - value
                                          type: object
                                        type: array
                                      path:
                                        description: Path-based matching. Only one
                                          of exact or suffix match can be specified.
                                        oneOf:
                                        - properties:
                                            exact: {}
                                          required:
                                          - exact
                                        - properties:
                                            prefix: {}
                                          required:
                                          - prefix
                                        properties:
                                          exact:
                                            description: Exact matching. The path
                                              must be exactly the same as the value.
                                            minLength: 1
                                            type: string
                                          prefix:
                                            description: Prefix matching. The path
                                              must start with the value.
                                            minLength: 1
                                            type: string
                                        type: object
                                      sourceCIDR:
                                        description: Source CIDR based matching. This
                                          allows for matching a specific or a range
                                          of IPv4 or IPv6 addresses.
                                        properties:
                                          cidr:
                                            description: |-
                                              CIDR of the source.
                                              This must be in CIDR notation and use a /32 to express
                                              a single host.
                                            format: cidr
                                            minLength: 1
                                            type: string
                                        required:
                                        - cidr
                                        type: object
                                    type: object
                                  minItems: 1
                                  type: array
                              required:
                              - ruleType
                              type: object
                          required:
                          - backendRef
                          type: object
                        minItems: 1
                        type: array
                    required:
                    - routes
                    type: object
                    x-kubernetes-validations:
                    - message: Application-wide and per-route auth type must be matched
                      rule: '!has(self.auth) || (has(self.auth.basic) && self.routes.all(r,
                        !has(r.auth) || has(r.auth.basic))) || (has(self.auth.jwt)   &&
                        self.routes.all(r, !has(r.auth) || has(r.auth.jwt)))'
                  httpsProxy:
                    description: |-
                      Defining this stanza enables HTTPSProxy application that proxies the
                      HTTPS traffic to the backends over TLS and TCP connections.
                    properties:
                      auth:
                        description: Optional HTTP authN/authZ configuration for the
                          HTTPS proxy application.
                        oneOf:
                        - properties:
                            basic: {}
                          required:
                          - basic
                        - properties:
                            jwt: {}
                          required:
                          - jwt
                        properties:
                          basic:
                            description: The basic authentication configuration.
                            properties:
                              users:
                                description: Users for the basic authentication.
                                properties:
                                  secretRef:
                                    description: |-
                                      The reference to the k8s secret that contains the username and
                                      password for the basic authentication. This must be a k8s secret of
                                      type Opaque with the username as a key and the password as a value.
                                      A single secret can contain multiple username-password pairs.
                                    properties:
                                      name:
                                        description: The name of the K8s Secret resource.
                                        minLength: 1
                                        type: string
                                    required:
                                    - name
                                    type: object
                                required:
                                - secretRef
                                type: object
                            required:
                            - users
                            type: object
                          jwt:
                            description: The jwt authentication configuration.
                            properties:
                              providers:
                                description: |-
                                  The list of JWT authentication providers. At least one provider must
                                  be specified.
                                items:
                                  properties:
                                    audiences:
                                      description: |-
                                        List of expected audiences. If specified, one of the audience has to
                                        match the aud claim in the JWT. Otherwise, the aud field is not
                                        checked.
                                      items:
                                        type: string
                                      maxItems: 8
                                      type: array
                                    issuer:
                                      description: |-
                                        An expected issuer (iss) of the JWT. If specified, it has to match
                                        the iss claim in the JWT. Otherwise, the iss field is not checked.
                                      minLength: 1
                                      type: string
                                    jwks:
                                      description: The JWT authentication configuration.
                                      properties:
                                        httpURI:
                                          description: Get JWKS from remote server
                                            with HTTP.
                                          properties:
                                            uri:
                                              description: The remote HTTP URI. The
                                                scheme must be http or https.
                                              format: uri
                                              type: string
                                              x-kubernetes-validations:
                                              - message: The scheme must be http or
                                                  https
                                                rule: self.startsWith('http') || self.startsWith('https')
                                          required:
                                          - uri
                                          type: object
                                        secretRef:
                                          description: |-
                                            The reference to the k8s Secret contains JWKS. The Secret must be an
                                            Opaque Secret with "jwks" key and base64-encoded JWKS string as a
                                            value.
                                          properties:
                                            name:
                                              description: The name of the K8s Secret
                                                resource.
                                              minLength: 1
                                              type: string
                                          required:
                                          - name
                                          type: object
                                      type: object
                                      x-kubernetes-validations:
                                      - message: Either secretRef or httpURI must
                                          be specified
                                        rule: (has(self.secretRef)?1:0)+(has(self.httpURI)?1:0)==1
                                    name:
                                      description: The name of the provider.
                                      type: string
                                  required:
                                  - jwks
                                  - name
                                  type: object
                                maxItems: 8
                                minItems: 1
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                            required:
                            - providers
                            type: object
                        type: object
                      connectionFiltering:
                        description: |-
                          The optional connection filtering configuration for all HTTPS connections.
                          It defines the connection attributes that should be obtained to decide
                          whether connections should be denied or allowed.
                        properties:
                          ruleType:
                            description: The type of the rules.
                            enum:
                            - Allow
                            - Deny
                            type: string
                          rules:
                            description: |-
                              Configure the rules that should be used for the HTTP application.
                              Each rule needs to define at least one property to filter on.
                              The properties of each entry are logically ANDed.
                            items:
                              anyOf:
                              - properties:
                                  sourceCIDR: {}
                                required:
                                - sourceCIDR
                              properties:
                                sourceCIDR:
                                  description: Source CIDR based matching. This allows
                                    for matching a specific or a range of IPv4 or
                                    IPv6 addresses.
                                  properties:
                                    cidr:
                                      description: |-
                                        CIDR of the source.
                                        This must be in CIDR notation and use a /32 to express
                                        a single host.
                                      format: cidr
                                      minLength: 1
                                      type: string
                                  required:
                                  - cidr
                                  type: object
                              type: object
                            minItems: 1
                            type: array
                        required:
                        - ruleType
                        type: object
                      httpConfig:
                        anyOf:
                        - properties:
                            enableHTTP11: {}
                          required:
                          - enableHTTP11
                        - properties:
                            enableHTTP2: {}
                          required:
                          - enableHTTP2
                        description: The application-wide HTTP configuration.
                        properties:
                          enableHTTP2:
                            description: Setting this to true enables HTTP2.
                            type: boolean
                          enableHTTP11:
                            description: Setting this to true enables HTTP/1.1.
                            type: boolean
                        type: object
                      rateLimits:
                        anyOf:
                        - properties:
                            connections: {}
                          required:
                          - connections
                        description: |-
                          Optional global rate limit configuration for the HTTPS proxy application.
                          Currently, this is only a local rate limit (enforced on each LB node individually).
                        properties:
                          connections:
                            description: |-
                              Rate limiting on connection basis.
                              It is applied and enforced when the connection is established.
                            properties:
                              limit:
                                minimum: 1
                                type: integer
                              timePeriodSeconds:
                                minimum: 1
                                type: integer
                            required:
                            - limit
                            - timePeriodSeconds
                            type: object
                        type: object
                      routes:
                        description: The HTTP routing configuration.
                        items:
                          properties:
                            auth:
                              description: Optional per-route authN/authZ configuration.
                              properties:
                                basic:
                                  description: The per-route basic authentication
                                    configuration.
                                  properties:
                                    disabled:
                                      description: Disables the basic authentication
                                        for this route.
                                      type: boolean
                                  required:
                                  - disabled
                                  type: object
                                jwt:
                                  description: The per-route JWT authentication configuration.
                                  properties:
                                    disabled:
                                      description: Disables the JWT authentication
                                        for this route.
                                      type: boolean
                                  required:
                                  - disabled
                                  type: object
                              type: object
                            backendRef:
                              description: |-
                                The reference to the LBBackendPool resource that this route should
                                forward the traffic to when the route is matched. The referred
                                LBBackendPool must exist in the same namespace as the LBService.
                              properties:
                                name:
                                  description: The name of the LBBackendPool resource.
                                  minLength: 1
                                  type: string
                              required:
                              - name
                              type: object
                            match:
                              description: |-
                                The HTTP route matching criteria. All conditions must be satisfied
                                for the route to be matched.
                              properties:
                                hostNames:
                                  description: |-
                                    The list of host names that the route should match. The host name is
                                    the value of the Host header in the HTTP request for plain-text
                                    HTTP. When TLS is enabled, the host name must match both the SNI and
                                    the Host header. The following formats are supported:

                                    - Exact domain names: www.example.com
                                    - Suffix domain wildcards: *.example.com
                                    - Special wildcard: * matching any domain

                                    Omitting this field is identical to specifying a wildcard "*".
                                  items:
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                    type: string
                                  type: array
                                path:
                                  description: |-
                                    The path matching criteria. When omitted, the route matches all
                                    paths.
                                  oneOf:
                                  - properties:
                                      exact: {}
                                    required:
                                    - exact
                                  - properties:
                                      prefix: {}
                                    required:
                                    - prefix
                                  properties:
                                    exact:
                                      description: Exact matching. The path must be
                                        exactly the same as the value.
                                      minLength: 1
                                      type: string
                                    prefix:
                                      description: Prefix matching. The path must
                                        start with the value.
                                      minLength: 1
                                      type: string
                                  type: object
                              type: object
                            persistentBackend:
                              anyOf:
                              - properties:
                                  sourceIP: {}
                                required:
                                - sourceIP
                              - properties:
                                  cookies: {}
                                required:
                                - cookies
                              - properties:
                                  headers: {}
                                required:
                                - headers
                              description: |-
                                The optional persistent backend configuration for this HTTP route.
                                It defines the request attributes that should be obtained to decide
                                whether requests should be sent to persistently the same backend.
                                The attributes are logically ANDed.

                                Note: Persistent backend configuration is only supported by LBBackendPools
                                with loadbalancing algorithm `consistentHashing`.
                              properties:
                                cookies:
                                  description: List of cookies for which requests
                                    are sent to the same backend if they match.
                                  items:
                                    properties:
                                      name:
                                        description: The name of the cookie that will
                                          be used to obtain the hash key.
                                        type: string
                                    required:
                                    - name
                                    type: object
                                  type: array
                                headers:
                                  description: List of headers for which requests
                                    are sent to the same backend if they match.
                                  items:
                                    properties:
                                      name:
                                        description: The name of the header that will
                                          be used to obtain the hash key.
                                        type: string
                                    required:
                                    - name
                                    type: object
                                  type: array
                                sourceIP:
                                  description: |-
                                    Whether requests from the same source IP should be sent to
                                    the same backend.
                                  type: boolean
                              type: object
                            rateLimits:
                              anyOf:
                              - properties:
                                  requests: {}
                                required:
                                - requests
                              description: |-
                                Optional per-route rate limit configuration.
                                Currently, this is only a local rate limit (enforced on each LB node individually).
                              properties:
                                requests:
                                  description: |-
                                    Configure max allowed requests for the HTTP route.
                                    It is applied and enforced before routing the HTTP request.
                                  properties:
                                    limit:
                                      minimum: 1
                                      type: integer
                                    timePeriodSeconds:
                                      minimum: 1
                                      type: integer
                                  required:
                                  - limit
                                  - timePeriodSeconds
                                  type: object
                              type: object
                            requestFiltering:
                              description: |-
                                The optional request filtering configuration for this HTTP route.
                                It defines the request attributes that should be obtained to decide
                                whether requests should be denied or allowed.
                              properties:
                                ruleType:
                                  description: The type of the rules.
                                  enum:
                                  - Allow
                                  - Deny
                                  type: string
                                rules:
                                  description: |-
                                    Configure the rules that should be used for the HTTP route.
                                    Each rule needs to define at least one property to filter on.
                                    The properties of each entry are logically ANDed.
                                  items:
                                    anyOf:
                                    - properties:
                                        sourceCIDR: {}
                                      required:
                                      - sourceCIDR
                                    - properties:
                                        clientCertificateSANs: {}
                                      required:
                                      - clientCertificateSANs
                                    - properties:
                                        hostName: {}
                                      required:
                                      - hostName
                                    - properties:
                                        path: {}
                                      required:
                                      - path
                                    - properties:
                                        headers: {}
                                      required:
                                      - headers
                                    - properties:
                                        jwtClaims: {}
                                      required:
                                      - jwtClaims
                                    properties:
                                      clientCertificateSANs:
                                        description: Client certificate SAN based
                                          matching. Only one of exact, suffix or regex
                                          match can be specified.
                                        items:
                                          properties:
                                            oid:
                                              description: |-
                                                OID Value which is required if OTHER_NAME SAN type is used.
                                                For example, UPN OID is 1.3.6.1.4.1.311.20.2.3
                                                (Reference: http://oid-info.com/get/1.3.6.1.4.1.311.20.2.3).

                                                If set for SAN types other than OTHER_NAME, it will be ignored.
                                              type: string
                                            type:
                                              description: Type of the SAN.
                                              enum:
                                              - DNS
                                              - URI
                                              - EMAIL
                                              - IP_ADDRESS
                                              - OTHER_NAME
                                              type: string
                                            value:
                                              description: Value of the SAN.
                                              oneOf:
                                              - properties:
                                                  exact: {}
                                                required:
                                                - exact
                                              - properties:
                                                  prefix: {}
                                                required:
                                                - prefix
                                              - properties:
                                                  regex: {}
                                                required:
                                                - regex
                                              properties:
                                                exact:
                                                  description: Exact matching. The
                                                    value must be exactly the same
                                                    as the value.
                                                  minLength: 1
                                                  type: string
                                                prefix:
                                                  description: Prefix matching. The
                                                    value must start with the value.
                                                  minLength: 1
                                                  type: string
                                                regex:
                                                  description: Regex matching. The
                                                    value must match the regex value.
                                                  minLength: 1
                                                  type: string
                                              type: object
                                          required:
                                          - type
                                          - value
                                          type: object
                                          x-kubernetes-validations:
                                          - message: OID needs to be set for SAN type
                                              OTHER_NAME
                                            rule: (self.type != 'OTHER_NAME' || has(self.oid))
                                        type: array
                                      headers:
                                        description: Header-based matching. Only one
                                          of exact, suffix or regex match can be specified.
                                        items:
                                          properties:
                                            name:
                                              description: Name of the header.
                                              minLength: 1
                                              type: string
                                            value:
                                              description: Value of the header.
                                              oneOf:
                                              - properties:
                                                  exact: {}
                                                required:
                                                - exact
                                              - properties:
                                                  prefix: {}
                                                required:
                                                - prefix
                                              - properties:
                                                  regex: {}
                                                required:
                                                - regex
                                              properties:
                                                exact:
                                                  description: Exact matching. The
                                                    value must be exactly the same
                                                    as the value.
                                                  minLength: 1
                                                  type: string
                                                prefix:
                                                  description: Prefix matching. The
                                                    value must start with the value.
                                                  minLength: 1
                                                  type: string
                                                regex:
                                                  description: Regex matching. The
                                                    value must match the regex value.
                                                  minLength: 1
                                                  type: string
                                              type: object
                                          required:
                                          - name
                                          - value
                                          type: object
                                        type: array
                                      hostName:
                                        description: Host-based matching. Only one
                                          of exact or suffix match can be specified.
                                        oneOf:
                                        - properties:
                                            exact: {}
                                          required:
                                          - exact
                                        - properties:
                                            suffix: {}
                                          required:
                                          - suffix
                                        properties:
                                          exact:
                                            description: Exact matching. The hostname
                                              must be exactly the same as the value.
                                            minLength: 1
                                            type: string
                                          suffix:
                                            description: Suffix matching. The hostname
                                              must end with the value.
                                            minLength: 1
                                            type: string
                                        type: object
                                      jwtClaims:
                                        description: JWT claim based matching. Only
                                          one of exact, suffix or regex match can
                                          be specified.
                                        items:
                                          properties:
                                            name:
                                              description: Name of the JWT claim.
                                              minLength: 1
                                              type: string
                                            value:
                                              description: Value of the JWT claim.
                                              oneOf:
                                              - properties:
                                                  exact: {}
                                                required:
                                                - exact
                                              - properties:
                                                  prefix: {}
                                                required:
                                                - prefix
                                              - properties:
                                                  regex: {}
                                                required:
                                                - regex
                                              properties:
                                                exact:
                                                  description: Exact matching. The
                                                    value must be exactly the same
                                                    as the value.
                                                  minLength: 1
                                                  type: string
                                                prefix:
                                                  description: Prefix matching. The
                                                    value must start with the value.
                                                  minLength: 1
                                                  type: string
                                                regex:
                                                  description: Regex matching. The
                                                    value must match the regex value.
                                                  minLength: 1
                                                  type: string
                                              type: object
                                          required:
                                          - name
                                          - value
                                          type: object
                                        type: array
                                      path:
                                        description: Path-based matching. Only one
                                          of exact or suffix match can be specified.
                                        oneOf:
                                        - properties:
                                            exact: {}
                                          required:
                                          - exact
                                        - properties:
                                            prefix: {}
                                          required:
                                          - prefix
                                        properties:
                                          exact:
                                            description: Exact matching. The path
                                              must be exactly the same as the value.
                                            minLength: 1
                                            type: string
                                          prefix:
                                            description: Prefix matching. The path
                                              must start with the value.
                                            minLength: 1
                                            type: string
                                        type: object
                                      sourceCIDR:
                                        description: Source CIDR based matching. This
                                          allows for matching a specific or a range
                                          of IPv4 or IPv6 addresses.
                                        properties:
                                          cidr:
                                            description: |-
                                              CIDR of the source.
                                              This must be in CIDR notation and use a /32 to express
                                              a single host.
                                            format: cidr
                                            minLength: 1
                                            type: string
                                        required:
                                        - cidr
                                        type: object
                                    type: object
                                  minItems: 1
                                  type: array
                              required:
                              - ruleType
                              type: object
                          required:
                          - backendRef
                          type: object
                        minItems: 1
                        type: array
                      tlsConfig:
                        description: The application-wide TLS configuration.
                        properties:
                          allowedCipherSuites:
                            description: |-
                              Allowed TLS cipher suites.

                              If not defined, the defaults of the Envoy proxy are used.
                              https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/transport_sockets/tls/v3/common.proto#extensions-transport-sockets-tls-v3-tlsparameters
                            items:
                              minLength: 1
                              type: string
                            type: array
                          allowedECDHCurves:
                            description: |-
                              Allowed ECDH Curves.

                              If not defined, the defaults of the Envoy proxy are used.
                              https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/transport_sockets/tls/v3/common.proto#extensions-transport-sockets-tls-v3-tlsparameters
                            items:
                              minLength: 1
                              type: string
                            type: array
                          allowedSignatureAlgorithms:
                            description: |-
                              Allowed signature algorithms. The list is ordered by preference.

                              If not defined, the defaults of the Envoy proxy are used.
                              https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/transport_sockets/tls/v3/common.proto#extensions-transport-sockets-tls-v3-tlsparameters
                            items:
                              minLength: 1
                              type: string
                            type: array
                          certificates:
                            description: The list of certificates that the service
                              uses.
                            items:
                              properties:
                                secretRef:
                                  description: |-
                                    Reference to K8s secret in the same namespace that contains
                                    the certificate and the private key that the service uses.

                                    The secret type must be kubernetes.io/tls and the
                                    format must follow the spec.

                                    https://kubernetes.io/docs/concepts/configuration/secret/#tls-secrets
                                  properties:
                                    name:
                                      description: The name of the K8s Secret resource.
                                      minLength: 1
                                      type: string
                                  required:
                                  - name
                                  type: object
                              required:
                              - secretRef
                              type: object
                            minItems: 1
                            type: array
                          maxTLSVersion:
                            description: |-
                              Maximum TLS version.

                              If not defined, the defaults of the Envoy proxy are used.
                              https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/transport_sockets/tls/v3/common.proto#extensions-transport-sockets-tls-v3-tlsparameters
                            enum:
                            - TLSv1_0
                            - TLSv1_1
                            - TLSv1_2
                            - TLSv1_3
                            type: string
                          minTLSVersion:
                            description: |-
                              Minimum TLS version.

                              If not defined, the defaults of the Envoy proxy are used.
                              https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/transport_sockets/tls/v3/common.proto#extensions-transport-sockets-tls-v3-tlsparameters
                            enum:
                            - TLSv1_0
                            - TLSv1_1
                            - TLSv1_2
                            - TLSv1_3
                            type: string
                          validation:
                            description: Optional certificate verification.
                            properties:
                              secretRef:
                                description: The k8s secret that contains the trusted
                                  CA in the secret data field `ca.crt`.
                                properties:
                                  name:
                                    description: The name of the K8s Secret resource.
                                    minLength: 1
                                    type: string
                                required:
                                - name
                                type: object
                              subjectAlternativeNames:
                                description: Allowed subject alternative names
                                items:
                                  properties:
                                    exact:
                                      description: Exact matching. The SAN must be
                                        exactly the same as the value.
                                      minLength: 1
                                      type: string
                                  required:
                                  - exact
                                  type: object
                                type: array
                            required:
                            - secretRef
                            type: object
                        required:
                        - certificates
                        type: object
                    required:
                    - routes
                    - tlsConfig
                    type: object
                    x-kubernetes-validations:
                    - message: Application-wide and per-route auth type must be matched
                      rule: '!has(self.auth) || (has(self.auth.basic) && self.routes.all(r,
                        !has(r.auth) || has(r.auth.basic))) || (has(self.auth.jwt)   &&
                        self.routes.all(r, !has(r.auth) || has(r.auth.jwt)))'
                  tcpProxy:
                    description: |-
                      Defining this stanza enables TCPProxy application that proxies the
                      TCP traffic to the backends by terminating the TCP.
                    properties:
                      forceDeploymentMode:
                        description: |-
                          Enforces specific implementation to be used to realize
                          TCPProxy application. This configuration should be used
                          only when there's a performance issue or bugs in the
                          implementation chosen by the default "auto" option.

                          Following options are available:

                          auto    : The LB controller automatically chooses the most
                                    efficient implementation for given configuration.

                          t1-only : Enforces TCPProxy to be realized in T1 nodes only.
                                    If there is any LBService or LBBackendPool configuration
                                    incompatible with T1 capability, an error will be
                                    reported.

                          t1-t2   : Enforces TCPProxy to be realized in T1 and T2 nodes
                                    even if it can be fully realized in T1 nodes only.

                          Optional, Default: auto
                        enum:
                        - auto
                        - t1-only
                        - t1-t2
                        type: string
                      routes:
                        description: The TCP proxy routing configuration.
                        items:
                          properties:
                            backendRef:
                              description: |-
                                The reference to the LBBackendPool resource that this route should
                                forward the traffic to when the route is matched. The referred
                                LBBackendPool must exist in the same namespace as the LBService.
                              properties:
                                name:
                                  description: The name of the LBBackendPool resource.
                                  minLength: 1
                                  type: string
                              required:
                              - name
                              type: object
                            connectionFiltering:
                              description: |-
                                The optional connection filtering configuration for this TCP route.
                                It defines the connection attributes that should be obtained to decide
                                whether connections should be denied or allowed.
                              properties:
                                ruleType:
                                  description: The type of the rules.
                                  enum:
                                  - Allow
                                  - Deny
                                  type: string
                                rules:
                                  description: |-
                                    Configure the rules that should be used for the TCP route.
                                    Each rule needs to define at least one property to filter on.
                                    The properties of each entry are logically ANDed.
                                  items:
                                    anyOf:
                                    - properties:
                                        sourceCIDR: {}
                                      required:
                                      - sourceCIDR
                                    properties:
                                      sourceCIDR:
                                        description: Source CIDR based matching. This
                                          allows for matching a specific or a range
                                          of IPv4 or IPv6 addresses.
                                        properties:
                                          cidr:
                                            description: |-
                                              CIDR of the source.
                                              This must be in CIDR notation and use a /32 to express
                                              a single host.
                                            format: cidr
                                            minLength: 1
                                            type: string
                                        required:
                                        - cidr
                                        type: object
                                    type: object
                                  minItems: 1
                                  type: array
                              required:
                              - ruleType
                              type: object
                            persistentBackend:
                              anyOf:
                              - properties:
                                  sourceIP: {}
                                required:
                                - sourceIP
                              description: |-
                                The optional persistent backend configuration for this TCP route.
                                It defines the request attributes that should be obtained to decide
                                whether requests should be sent to persistently the same backend.
                                The attributes are logically ANDed.

                                Note: Persistent backend configuration is only supported by LBBackendPools
                                with loadbalancing algorithm `consistentHashing`.
                              properties:
                                sourceIP:
                                  description: |-
                                    Whether requests from the same source IP should be sent to
                                    the same backend.
                                  type: boolean
                              type: object
                            rateLimits:
                              anyOf:
                              - properties:
                                  connections: {}
                                required:
                                - connections
                              description: |-
                                Optional per-route rate limit configuration.
                                Currently, this is only a local rate limit (enforced on each LB node individually).
                              properties:
                                connections:
                                  description: |-
                                    Configure max allowed connections for the TCP route.
                                    It is applied and enforced when the connection is established.
                                  properties:
                                    limit:
                                      minimum: 1
                                      type: integer
                                    timePeriodSeconds:
                                      minimum: 1
                                      type: integer
                                  required:
                                  - limit
                                  - timePeriodSeconds
                                  type: object
                              type: object
                          required:
                          - backendRef
                          type: object
                        maxItems: 1
                        minItems: 1
                        type: array
                    required:
                    - routes
                    type: object
                    x-kubernetes-validations:
                    - message: Force deployment mode t1-only isn't compatible with
                        rate limits
                      rule: (!has(self.forceDeploymentMode) || self.forceDeploymentMode
                        == 'auto' || self.forceDeploymentMode == 't1-t2' || self.routes.all(x,
                        !has(x.rateLimits)) )
                  tlsPassthrough:
                    description: |-
                      Defining this stanza enables TLSPassthrough application that proxies
                      the TLS traffic without terminating the TLS connection.
                    properties:
                      routes:
                        description: The TLS passthrough routing configuration.
                        items:
                          properties:
                            backendRef:
                              description: |-
                                The reference to the LBBackendPool resource that this route should
                                forward the traffic to when the route is matched. The referred
                                LBBackendPool must exist in the same namespace as the LBService.
                              properties:
                                name:
                                  description: The name of the LBBackendPool resource.
                                  minLength: 1
                                  type: string
                              required:
                              - name
                              type: object
                            connectionFiltering:
                              description: |-
                                The optional connection filtering configuration for this TLS passthrough route.
                                It defines the connection attributes that should be obtained to decide
                                whether connections should be denied or allowed.
                              properties:
                                ruleType:
                                  description: The type of the rules.
                                  enum:
                                  - Allow
                                  - Deny
                                  type: string
                                rules:
                                  description: |-
                                    Configure the rules that should be used for the TLS route.
                                    Each rule needs to define at least one property to filter on.
                                    The properties of each entry are logically ANDed.
                                  items:
                                    anyOf:
                                    - properties:
                                        sourceCIDR: {}
                                      required:
                                      - sourceCIDR
                                    - properties:
                                        clientCertificateSANs: {}
                                      required:
                                      - clientCertificateSANs
                                    - properties:
                                        serverName: {}
                                      required:
                                      - serverName
                                    properties:
                                      clientCertificateSANs:
                                        description: Client certificate SAN based
                                          matching. Only one of exact, suffix or regex
                                          match can be specified.
                                        items:
                                          properties:
                                            oid:
                                              description: |-
                                                OID Value which is required if OTHER_NAME SAN type is used.
                                                For example, UPN OID is 1.3.6.1.4.1.311.20.2.3
                                                (Reference: http://oid-info.com/get/1.3.6.1.4.1.311.20.2.3).

                                                If set for SAN types other than OTHER_NAME, it will be ignored.
                                              type: string
                                            type:
                                              description: Type of the SAN.
                                              enum:
                                              - DNS
                                              - URI
                                              - EMAIL
                                              - IP_ADDRESS
                                              - OTHER_NAME
                                              type: string
                                            value:
                                              description: Value of the SAN.
                                              oneOf:
                                              - properties:
                                                  exact: {}
                                                required:
                                                - exact
                                              - properties:
                                                  prefix: {}
                                                required:
                                                - prefix
                                              - properties:
                                                  regex: {}
                                                required:
                                                - regex
                                              properties:
                                                exact:
                                                  description: Exact matching. The
                                                    value must be exactly the same
                                                    as the value.
                                                  minLength: 1
                                                  type: string
                                                prefix:
                                                  description: Prefix matching. The
                                                    value must start with the value.
                                                  minLength: 1
                                                  type: string
                                                regex:
                                                  description: Regex matching. The
                                                    value must match the regex value.
                                                  minLength: 1
                                                  type: string
                                              type: object
                                          required:
                                          - type
                                          - value
                                          type: object
                                          x-kubernetes-validations:
                                          - message: OID needs to be set for SAN type
                                              OTHER_NAME
                                            rule: (self.type != 'OTHER_NAME' || has(self.oid))
                                        type: array
                                      serverName:
                                        description: Servername-based matching. Only
                                          one of exact or suffix match can be specified.
                                        oneOf:
                                        - properties:
                                            exact: {}
                                          required:
                                          - exact
                                        - properties:
                                            suffix: {}
                                          required:
                                          - suffix
                                        properties:
                                          exact:
                                            description: Exact matching. The servername
                                              must be exactly the same as the value.
                                            minLength: 1
                                            type: string
                                          suffix:
                                            description: Suffix matching. The servername
                                              must end with the value.
                                            minLength: 1
                                            type: string
                                        type: object
                                      sourceCIDR:
                                        description: Source CIDR based matching. This
                                          allows for matching a specific or a range
                                          of IPv4 or IPv6 addresses.
                                        properties:
                                          cidr:
                                            description: |-
                                              CIDR of the source.
                                              This must be in CIDR notation and use a /32 to express
                                              a single host.
                                            format: cidr
                                            minLength: 1
                                            type: string
                                        required:
                                        - cidr
                                        type: object
                                    type: object
                                  minItems: 1
                                  type: array
                              required:
                              - ruleType
                              type: object
                            match:
                              description: |-
                                The TLS route matching criteria. All conditions must be satisfied
                                for the route to be matched.
                              properties:
                                hostNames:
                                  description: |-
                                    The list of host names that the route should match. The host name
                                    must match the SNI. The following formats are supported:

                                    - Exact domain names: www.example.com
                                    - Suffix domain wildcards: *.example.com
                                    - Special wildcard: * matching any domain

                                    Omitting this field is identical to specifying a wildcard "*".
                                  items:
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                    type: string
                                  type: array
                              type: object
                            persistentBackend:
                              anyOf:
                              - properties:
                                  sourceIP: {}
                                required:
                                - sourceIP
                              description: |-
                                The optional persistent backend configuration for this TLS passthrough route.
                                It defines the request attributes that should be obtained to decide
                                whether requests should be sent to persistently the same backend.
                                The attributes are logically ANDed.

                                Note: Persistent backend configuration is only supported by LBBackendPools
                                with loadbalancing algorithm `consistentHashing`.
                              properties:
                                sourceIP:
                                  description: |-
                                    Whether requests from the same source IP should be sent to
                                    the same backend.
                                  type: boolean
                              type: object
                            rateLimits:
                              anyOf:
                              - properties:
                                  connections: {}
                                required:
                                - connections
                              description: |-
                                Optional per-route rate limit configuration.
                                Currently, this is only a local rate limit (enforced on each LB node individually).
                              properties:
                                connections:
                                  description: |-
                                    Configure max allowed connections for the TLS route.
                                    It is applied and enforced when the connection is established.
                                  properties:
                                    limit:
                                      minimum: 1
                                      type: integer
                                    timePeriodSeconds:
                                      minimum: 1
                                      type: integer
                                  required:
                                  - limit
                                  - timePeriodSeconds
                                  type: object
                              type: object
                          required:
                          - backendRef
                          type: object
                        minItems: 1
                        type: array
                    required:
                    - routes
                    type: object
                  tlsProxy:
                    description: |-
                      Defining this stanza enables TLSProxy application that proxies the
                      TLS traffic to the backends by terminating the TLS.
                    properties:
                      routes:
                        description: The TLS proxy routing configuration.
                        items:
                          properties:
                            backendRef:
                              description: |-
                                The reference to the LBBackendPool resource that this route should
                                forward the traffic to when the route is matched. The referred
                                LBBackendPool must exist in the same namespace as the LBService.
                              properties:
                                name:
                                  description: The name of the LBBackendPool resource.
                                  minLength: 1
                                  type: string
                              required:
                              - name
                              type: object
                            connectionFiltering:
                              description: |-
                                The optional connection filtering configuration for this TLS proxy route.
                                It defines the connection attributes that should be obtained to decide
                                whether connections should be denied or allowed.
                              properties:
                                ruleType:
                                  description: The type of the rules.
                                  enum:
                                  - Allow
                                  - Deny
                                  type: string
                                rules:
                                  description: |-
                                    Configure the rules that should be used for the TLS route.
                                    Each rule needs to define at least one property to filter on.
                                    The properties of each entry are logically ANDed.
                                  items:
                                    anyOf:
                                    - properties:
                                        sourceCIDR: {}
                                      required:
                                      - sourceCIDR
                                    - properties:
                                        clientCertificateSANs: {}
                                      required:
                                      - clientCertificateSANs
                                    - properties:
                                        serverName: {}
                                      required:
                                      - serverName
                                    properties:
                                      clientCertificateSANs:
                                        description: Client certificate SAN based
                                          matching. Only one of exact, suffix or regex
                                          match can be specified.
                                        items:
                                          properties:
                                            oid:
                                              description: |-
                                                OID Value which is required if OTHER_NAME SAN type is used.
                                                For example, UPN OID is 1.3.6.1.4.1.311.20.2.3
                                                (Reference: http://oid-info.com/get/1.3.6.1.4.1.311.20.2.3).

                                                If set for SAN types other than OTHER_NAME, it will be ignored.
                                              type: string
                                            type:
                                              description: Type of the SAN.
                                              enum:
                                              - DNS
                                              - URI
                                              - EMAIL
                                              - IP_ADDRESS
                                              - OTHER_NAME
                                              type: string
                                            value:
                                              description: Value of the SAN.
                                              oneOf:
                                              - properties:
                                                  exact: {}
                                                required:
                                                - exact
                                              - properties:
                                                  prefix: {}
                                                required:
                                                - prefix
                                              - properties:
                                                  regex: {}
                                                required:
                                                - regex
                                              properties:
                                                exact:
                                                  description: Exact matching. The
                                                    value must be exactly the same
                                                    as the value.
                                                  minLength: 1
                                                  type: string
                                                prefix:
                                                  description: Prefix matching. The
                                                    value must start with the value.
                                                  minLength: 1
                                                  type: string
                                                regex:
                                                  description: Regex matching. The
                                                    value must match the regex value.
                                                  minLength: 1
                                                  type: string
                                              type: object
                                          required:
                                          - type
                                          - value
                                          type: object
                                          x-kubernetes-validations:
                                          - message: OID needs to be set for SAN type
                                              OTHER_NAME
                                            rule: (self.type != 'OTHER_NAME' || has(self.oid))
                                        type: array
                                      serverName:
                                        description: Servername-based matching. Only
                                          one of exact or suffix match can be specified.
                                        oneOf:
                                        - properties:
                                            exact: {}
                                          required:
                                          - exact
                                        - properties:
                                            suffix: {}
                                          required:
                                          - suffix
                                        properties:
                                          exact:
                                            description: Exact matching. The servername
                                              must be exactly the same as the value.
                                            minLength: 1
                                            type: string
                                          suffix:
                                            description: Suffix matching. The servername
                                              must end with the value.
                                            minLength: 1
                                            type: string
                                        type: object
                                      sourceCIDR:
                                        description: Source CIDR based matching. This
                                          allows for matching a specific or a range
                                          of IPv4 or IPv6 addresses.
                                        properties:
                                          cidr:
                                            description: |-
                                              CIDR of the source.
                                              This must be in CIDR notation and use a /32 to express
                                              a single host.
                                            format: cidr
                                            minLength: 1
                                            type: string
                                        required:
                                        - cidr
                                        type: object
                                    type: object
                                  minItems: 1
                                  type: array
                              required:
                              - ruleType
                              type: object
                            match:
                              description: |-
                                The TLS route matching criteria. All conditions must be satisfied
                                for the route to be matched.
                              properties:
                                hostNames:
                                  description: |-
                                    The list of host names that the route should match. The host name
                                    must match the SNI. The following formats are supported:

                                    - Exact domain names: www.example.com
                                    - Suffix domain wildcards: *.example.com
                                    - Special wildcard: * matching any domain

                                    Omitting this field is identical to specifying a wildcard "*".
                                  items:
                                    maxLength: 253
                                    minLength: 1
                                    pattern: ^(\*\.)?[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                                    type: string
                                  type: array
                              type: object
                            persistentBackend:
                              anyOf:
                              - properties:
                                  sourceIP: {}
                                required:
                                - sourceIP
                              description: |-
                                The optional persistent backend configuration for this TLS proxy route.
                                It defines the request attributes that should be obtained to decide
                                whether requests should be sent to persistently the same backend.
                                The attributes are logically ANDed.

                                Note: Persistent backend configuration is only supported by LBBackendPools
                                with loadbalancing algorithm `consistentHashing`.
                              properties:
                                sourceIP:
                                  description: |-
                                    Whether requests from the same source IP should be sent to
                                    the same backend.
                                  type: boolean
                              type: object
                            rateLimits:
                              anyOf:
                              - properties:
                                  connections: {}
                                required:
                                - connections
                              description: |-
                                Optional per-route rate limit configuration.
                                Currently, this is only a local rate limit (enforced on each LB node individually).
                              properties:
                                connections:
                                  description: |-
                                    Configure max allowed connections for the TLS route.
                                    It is applied and enforced when the connection is established.
                                  properties:
                                    limit:
                                      minimum: 1
                                      type: integer
                                    timePeriodSeconds:
                                      minimum: 1
                                      type: integer
                                  required:
                                  - limit
                                  - timePeriodSeconds
                                  type: object
                              type: object
                          required:
                          - backendRef
                          type: object
                        minItems: 1
                        type: array
                      tlsConfig:
                        description: The application-wide TLS configuration.
                        properties:
                          allowedCipherSuites:
                            description: |-
                              Allowed TLS cipher suites.

                              If not defined, the defaults of the Envoy proxy are used.
                              https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/transport_sockets/tls/v3/common.proto#extensions-transport-sockets-tls-v3-tlsparameters
                            items:
                              minLength: 1
                              type: string
                            type: array
                          allowedECDHCurves:
                            description: |-
                              Allowed ECDH Curves.

                              If not defined, the defaults of the Envoy proxy are used.
                              https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/transport_sockets/tls/v3/common.proto#extensions-transport-sockets-tls-v3-tlsparameters
                            items:
                              minLength: 1
                              type: string
                            type: array
                          allowedSignatureAlgorithms:
                            description: |-
                              Allowed signature algorithms. The list is ordered by preference.

                              If not defined, the defaults of the Envoy proxy are used.
                              https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/transport_sockets/tls/v3/common.proto#extensions-transport-sockets-tls-v3-tlsparameters
                            items:
                              minLength: 1
                              type: string
                            type: array
                          certificates:
                            description: The list of certificates that the service
                              uses.
                            items:
                              properties:
                                secretRef:
                                  description: |-
                                    Reference to K8s secret in the same namespace that contains
                                    the certificate and the private key that the service uses.

                                    The secret type must be kubernetes.io/tls and the
                                    format must follow the spec.

                                    https://kubernetes.io/docs/concepts/configuration/secret/#tls-secrets
                                  properties:
                                    name:
                                      description: The name of the K8s Secret resource.
                                      minLength: 1
                                      type: string
                                  required:
                                  - name
                                  type: object
                              required:
                              - secretRef
                              type: object
                            minItems: 1
                            type: array
                          maxTLSVersion:
                            description: |-
                              Maximum TLS version.

                              If not defined, the defaults of the Envoy proxy are used.
                              https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/transport_sockets/tls/v3/common.proto#extensions-transport-sockets-tls-v3-tlsparameters
                            enum:
                            - TLSv1_0
                            - TLSv1_1
                            - TLSv1_2
                            - TLSv1_3
                            type: string
                          minTLSVersion:
                            description: |-
                              Minimum TLS version.

                              If not defined, the defaults of the Envoy proxy are used.
                              https://www.envoyproxy.io/docs/envoy/latest/api-v3/extensions/transport_sockets/tls/v3/common.proto#extensions-transport-sockets-tls-v3-tlsparameters
                            enum:
                            - TLSv1_0
                            - TLSv1_1
                            - TLSv1_2
                            - TLSv1_3
                            type: string
                          validation:
                            description: Optional certificate verification.
                            properties:
                              secretRef:
                                description: The k8s secret that contains the trusted
                                  CA in the secret data field `ca.crt`.
                                properties:
                                  name:
                                    description: The name of the K8s Secret resource.
                                    minLength: 1
                                    type: string
                                required:
                                - name
                                type: object
                              subjectAlternativeNames:
                                description: Allowed subject alternative names
                                items:
                                  properties:
                                    exact:
                                      description: Exact matching. The SAN must be
                                        exactly the same as the value.
                                      minLength: 1
                                      type: string
                                  required:
                                  - exact
                                  type: object
                                type: array
                            required:
                            - secretRef
                            type: object
                        required:
                        - certificates
                        type: object
                    required:
                    - routes
                    - tlsConfig
                    type: object
                  udpProxy:
                    description: |-
                      Defining this stanza enables UDPProxy application that proxies the
                      UDP traffic to the backends by terminating the UDP.
                    properties:
                      forceDeploymentMode:
                        description: |-
                          Enforces specific implementation to be used to realize
                          UDPProxy application. This configuration should be used
                          only when there's a performance issue or bugs in the
                          implementation chosen by the default "auto" option.

                          Following options are available:

                          auto    : The LB controller automatically chooses the most
                                    efficient implementation for given configuration.

                          t1-only : Enforces UDPProxy to be realized in T1 nodes only.
                                    If there is any LBService or LBBackendPool configuration
                                    incompatible with T1 capability, an error will be
                                    reported.

                          t1-t2   : Enforces UDPProxy to be realized in T1 and T2 nodes
                                    even if it can be fully realized in T1 nodes only.

                          Optional, Default: auto
                        enum:
                        - auto
                        - t1-only
                        - t1-t2
                        type: string
                      routes:
                        description: The UDP proxy routing configuration.
                        items:
                          properties:
                            backendRef:
                              description: |-
                                The reference to the LBBackendPool resource that this route should
                                forward the traffic to when the route is matched. The referred
                                LBBackendPool must exist in the same namespace as the LBService.
                              properties:
                                name:
                                  description: The name of the LBBackendPool resource.
                                  minLength: 1
                                  type: string
                              required:
                              - name
                              type: object
                            connectionFiltering:
                              description: |-
                                The optional connection filtering configuration for this UDP route.
                                It defines the connection attributes that should be obtained to decide
                                whether connections should be denied or allowed.
                              properties:
                                ruleType:
                                  description: The type of the rules.
                                  enum:
                                  - Allow
                                  - Deny
                                  type: string
                                rules:
                                  description: |-
                                    Configure the rules that should be used for the UDP route.
                                    Each rule needs to define at least one property to filter on.
                                    The properties of each entry are logically ANDed.
                                  items:
                                    anyOf:
                                    - properties:
                                        sourceCIDR: {}
                                      required:
                                      - sourceCIDR
                                    properties:
                                      sourceCIDR:
                                        description: Source CIDR based matching. This
                                          allows for matching a specific or a range
                                          of IPv4 or IPv6 addresses.
                                        properties:
                                          cidr:
                                            description: |-
                                              CIDR of the source.
                                              This must be in CIDR notation and use a /32 to express
                                              a single host.
                                            format: cidr
                                            minLength: 1
                                            type: string
                                        required:
                                        - cidr
                                        type: object
                                    type: object
                                  minItems: 1
                                  type: array
                              required:
                              - ruleType
                              type: object
                            persistentBackend:
                              anyOf:
                              - properties:
                                  sourceIP: {}
                                required:
                                - sourceIP
                              description: |-
                                The optional persistent backend configuration for this UDP route.
                                It defines the request attributes that should be obtained to decide
                                whether requests should be sent to persistently the same backend.
                                The attributes are logically ANDed.

                                Note: Persistent backend configuration is only supported by LBBackendPools
                                with loadbalancing algorithm `consistentHashing`.
                              properties:
                                sourceIP:
                                  description: |-
                                    Whether requests from the same source IP should be sent to
                                    the same backend.
                                  type: boolean
                              type: object
                          required:
                          - backendRef
                          type: object
                        maxItems: 1
                        minItems: 1
                        type: array
                    required:
                    - routes
                    type: object
                type: object
              port:
                description: The port that this service should listen on.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              proxyProtocolConfig:
                description: |-
                  Enable PROXY protocol on this service. It can be enabled together
                  with TLS. If unspecified, PROXY protocol will be disabled.
                properties:
                  disallowedVersions:
                    description: |-
                      The list of versions of the PROXY protocol, which will be rejected.
                      If not specified, all versions are allowed.
                    items:
                      enum:
                      - 1
                      - 2
                      type: integer
                    minItems: 1
                    type: array
                  passthroughTLVs:
                    description: |-
                      This config controls which TLVs can be passed to filter state if it is Proxy Protocol
                      V2 header. If there is no setting for this field, no TLVs will be passed through.
                    items:
                      type: integer
                    minItems: 1
                    type: array
                type: object
              vipRef:
                description: |-
                  The reference to the LBVIP resource that the LBService should be
                  associated with. The referred LBVIP must exist in the same namespace
                  as the service. Multiple LBServices can refer to the same LBVIP to
                  share the same VIP, but the port must be different.
                properties:
                  name:
                    description: The name of the LBVIP resource.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - applications
            - port
            - vipRef
            type: object
          status:
            properties:
              addresses:
                description: |-
                  Allocated addresses for the LBService. The value is copied from the
                  LBVIP's status that the service refers to. This field exists for
                  the cosmetic purpose of showing the VIP in the kubectl output. You
                  should use LBVIP's status field as the source of truth.
                properties:
                  ipv4:
                    description: IPv4 VIP assigned to the LBService.
                    type: string
                  ipv6:
                    description: IPv6 VIP assigned to the LBService.
                    type: string
                type: object
              applications:
                description: |-
                  Contains additional status information for the configured applications.
                  While the name is plural, only one application can be specified
                  currently.
                properties:
                  tcpProxy:
                    description: Status information of the TCPProxy application.
                    properties:
                      deploymentMode:
                        description: |-
                          The applied deployment mode of the TCPProxy application.

                          This depends on the configured forceDeploymentMode on the TCPProxy application.
                          This is especially important for the mode `auto` where the actual
                          deployment mode is evaluated based on other configuration / enabled
                          features.
                        enum:
                        - t1-only
                        - t1-t2
                        type: string
                    type: object
                  udpProxy:
                    description: Status information of the UDPProxy application.
                    properties:
                      deploymentMode:
                        description: |-
                          The applied deployment mode of the UDPProxy application.

                          This depends on the configured forceDeploymentMode on the UDPProxy application.
                          This is especially important for the mode `auto` where the actual
                          deployment mode is evaluated based on other configuration / enabled
                          features.
                        enum:
                        - t1-only
                        - t1-t2
                        type: string
                    type: object
                type: object
              conditions:
                description: The current conditions of the LBService.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              k8sServiceRefs:
                description: |-
                  Contains K8s services that are referenced by all referenced
                  LBBackendPools.
                items:
                  properties:
                    name:
                      description: The name of the K8s Service resource.
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                type: array
              status:
                description: Status of the resource.
                enum:
                - OK
                - ConditionNotMet
                type: string
            required:
            - addresses
            - applications
            - status
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
