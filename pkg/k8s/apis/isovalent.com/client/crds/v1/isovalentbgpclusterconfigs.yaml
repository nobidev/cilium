---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: isovalentbgpclusterconfigs.isovalent.com
spec:
  group: isovalent.com
  names:
    categories:
    - cilium
    - isovalentbgp
    kind: IsovalentBGPClusterConfig
    listKind: IsovalentBGPClusterConfigList
    plural: isovalentbgpclusterconfigs
    shortNames:
    - ibgpcluster
    singular: isovalentbgpclusterconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: IsovalentBGPClusterConfig is the Schema for the IsovalentBGPClusterConfig
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired cluster configuration of the BGP
              control plane.
            properties:
              bgpInstances:
                description: |-
                  A list of IsovalentBGPInstance(s) which instructs
                  the BGP control plane how to instantiate virtual BGP routers.
                items:
                  properties:
                    localASN:
                      description: |-
                        LocalASN is the ASN of this BGP instance.
                        Supports extended 32bit ASNs.
                      format: int64
                      maximum: 4294967295
                      minimum: 1
                      type: integer
                    localPort:
                      description: |-
                        LocalPort is the port on which the BGP daemon listens for incoming connections.

                        If not specified, BGP instance will not listen for incoming connections.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    name:
                      description: |-
                        Name is the name of the BGP instance. It is a unique identifier for the BGP instance
                        within the cluster configuration.
                      maxLength: 255
                      minLength: 1
                      type: string
                    peers:
                      description: Peers is a list of neighboring BGP peers for this
                        virtual router
                      items:
                        description: IsovalentBGPPeer contains configuration for a
                          BGP peer.
                        properties:
                          autoDiscovery:
                            description: |-
                              AutoDiscovery allows auto-discovery of peer's IP address.
                              When a peer auto-discovery mechanism is enabled, the peerAddress field
                              can be empty (and will be ignored if set).
                            properties:
                              mode:
                                description: Mode defines the type of BGP peer auto-discovery
                                  mechanism.
                                enum:
                                - Unnumbered
                                type: string
                              unnumbered:
                                description: Unnumbered contains configuration for
                                  the BGP Unnumbered peer auto-discovery mode.
                                properties:
                                  interface:
                                    description: |-
                                      Interface is the name of an interface on the Cilium node to use for BGP unnumbered peering.

                                      The IPv6 link-local address of a neighbor discovered on the specified interface will be used for peering.
                                      Additionally, Router Advertisement messages will be sent out of the configured interface,
                                      so that the neighboring router can learn about Cilium node's link-local IPv6 address as well.
                                    type: string
                                required:
                                - interface
                                type: object
                            required:
                            - mode
                            type: object
                            x-kubernetes-validations:
                            - message: unnumbered field is required for the 'Unnumbered'
                                mode
                              rule: self.mode != 'Unnumbered' || has(self.unnumbered)
                          name:
                            description: Name is the name of the BGP peer. It is a
                              unique identifier for the peer within the BGP instance.
                            maxLength: 255
                            minLength: 1
                            type: string
                          peerASN:
                            default: 0
                            description: |-
                              PeerASN is the ASN of the peer BGP router.
                              Supports extended 32bit ASNs.

                              If peerASN is unspecified or 0, the BGP OPEN message validation of ASN
                              will be disabled and ASN will be determined based on peer's OPEN message.
                            format: int64
                            maximum: 4294967295
                            minimum: 0
                            type: integer
                          peerAddress:
                            description: |-
                              PeerAddress is the IP address of the neighbor.
                              Supports IPv4 and IPv6 addresses.
                            pattern: ((^\s*((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))\s*$)|(^\s*((([0-9A-Fa-f]{1,4}:){7}([0-9A-Fa-f]{1,4}|:))|(([0-9A-Fa-f]{1,4}:){6}(:[0-9A-Fa-f]{1,4}|((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3})|:))|(([0-9A-Fa-f]{1,4}:){5}(((:[0-9A-Fa-f]{1,4}){1,2})|:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3})|:))|(([0-9A-Fa-f]{1,4}:){4}(((:[0-9A-Fa-f]{1,4}){1,3})|((:[0-9A-Fa-f]{1,4})?:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(([0-9A-Fa-f]{1,4}:){3}(((:[0-9A-Fa-f]{1,4}){1,4})|((:[0-9A-Fa-f]{1,4}){0,2}:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(([0-9A-Fa-f]{1,4}:){2}(((:[0-9A-Fa-f]{1,4}){1,5})|((:[0-9A-Fa-f]{1,4}){0,3}:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(([0-9A-Fa-f]{1,4}:){1}(((:[0-9A-Fa-f]{1,4}){1,6})|((:[0-9A-Fa-f]{1,4}){0,4}:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(:(((:[0-9A-Fa-f]{1,4}){1,7})|((:[0-9A-Fa-f]{1,4}){0,5}:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:)))(%.+)?\s*$))
                            type: string
                          peerConfigRef:
                            description: |-
                              PeerConfigRef is a reference to a peer configuration resource.
                              If not specified, the default BGP configuration is used for this peer.
                            properties:
                              name:
                                description: |-
                                  Name is the name of the peer config resource.
                                  Name refers to the name of a Kubernetes object (typically a IsovalentBGPPeerConfig).
                                type: string
                            required:
                            - name
                            type: object
                        required:
                        - name
                        type: object
                        x-kubernetes-validations:
                        - message: Either peerAddress or autoDiscovery must be specified
                          rule: has(self.peerAddress) || has(self.autoDiscovery)
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    routeReflector:
                      description: |-
                        RouteReflector defines which route reflector cluster this instance
                        joins. When specified, this instance automatically peers with the
                        route reflectors or route reflector clients in the cluster.
                      properties:
                        clusterID:
                          description: |-
                            ClusterID is the ID of the route reflector cluster that this
                            instance joins.
                          format: ipv4
                          type: string
                        peerConfigRefV4:
                          description: |-
                            PeerConfigRefV4 is a reference to the IsovalentBGPPeerConfig when
                            this instance peers with other peers in the same route reflector
                            cluster with IPv4. Only valid when the peeringAddressFamily is
                            `IPv4Only` or `Dual`.
                          properties:
                            name:
                              description: |-
                                Name is the name of the peer config resource.
                                Name refers to the name of a Kubernetes object (typically a IsovalentBGPPeerConfig).
                              type: string
                          required:
                          - name
                          type: object
                        peerConfigRefV6:
                          description: |-
                            PeerConfigRefV6 is a reference to the IsovalentBGPPeerConfig when
                            this instance peers with other peers in the same route reflector
                            cluster with IPv6. Only valid when the peeringAddressFamily is
                            `IPv6Only` or `Dual`.
                          properties:
                            name:
                              description: |-
                                Name is the name of the peer config resource.
                                Name refers to the name of a Kubernetes object (typically a IsovalentBGPPeerConfig).
                              type: string
                          required:
                          - name
                          type: object
                        peeringAddressFamily:
                          description: |-
                            PeeringAddressFamily controls the way how route reflectors and
                            clients make peering session(s). Available options are following:

                            - IPv4Only: Makes single IPv4 peering session
                            - IPv6Only: Makes single IPv6 peering session
                            - Dual: Makes both IPv4 and IPv6 peering sessions

                            If omitted, the default value will be selected based on the Cilium's
                            configuration. When Cilium is configured as IPv4 single-stack or
                            IPv6 single-stack, the default are `IPv4Only` and `IPv6Only`
                            respectively. If Cilium is configured as dual-stack, the default is
                            `Dual`.
                          enum:
                          - IPv4Only
                          - IPv6Only
                          - Dual
                          type: string
                        role:
                          description: Role is a role of the instance within the RR
                            cluster
                          enum:
                          - RouteReflector
                          - Client
                          type: string
                      required:
                      - clusterID
                      - role
                      type: object
                    vrfs:
                      description: VRFs is a list of VRFs for this virtual router
                      items:
                        properties:
                          configRef:
                            description: ConfigRef is a reference to a IsovalentBGPVRFConfig
                              resource.
                            type: string
                          exportRTs:
                            description: ExportRTs is a list of route targets to export
                              routes to.
                            items:
                              type: string
                            type: array
                          importRTs:
                            description: ImportRTs is a list of route targets to import
                              routes from.
                            items:
                              type: string
                            type: array
                          rd:
                            description: RD is the Route Distinguisher of the VRF.
                            type: string
                          vrfRef:
                            description: |-
                              VRFRef is a reference to a IsovalentVRF resource. It should be the same as the name of the
                              IsovalentVRF object to which this BGPVRF is associated.
                            maxLength: 255
                            minLength: 1
                            type: string
                        required:
                        - vrfRef
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - vrfRef
                      x-kubernetes-list-type: map
                  required:
                  - name
                  type: object
                maxItems: 16
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              nodeSelector:
                description: |-
                  NodeSelector selects a group of nodes where this BGP Cluster
                  config applies.
                  If empty / nil this config applies to all nodes.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          enum:
                          - In
                          - NotIn
                          - Exists
                          - DoesNotExist
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      description: MatchLabelsValue represents the value from the
                        MatchLabels {key,value} pair.
                      maxLength: 63
                      pattern: ^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - bgpInstances
            type: object
          status:
            description: Status is a running status of the cluster configuration
            properties:
              conditions:
                description: The current conditions of the IsovalentBGPClusterConfig
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    deprecated: true
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IsovalentBGPClusterConfig is the Schema for the IsovalentBGPClusterConfig
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired cluster configuration of the BGP
              control plane.
            properties:
              bgpInstances:
                description: |-
                  A list of IsovalentBGPInstance(s) which instructs
                  the BGP control plane how to instantiate virtual BGP routers.
                items:
                  properties:
                    localASN:
                      description: |-
                        LocalASN is the ASN of this BGP instance.
                        Supports extended 32bit ASNs.
                      format: int64
                      maximum: 4294967295
                      minimum: 1
                      type: integer
                    name:
                      description: |-
                        Name is the name of the BGP instance. It is a unique identifier for the BGP instance
                        within the cluster configuration.
                      maxLength: 255
                      minLength: 1
                      type: string
                    peers:
                      description: Peers is a list of neighboring BGP peers for this
                        virtual router
                      items:
                        description: IsovalentBGPPeer contains configuration of a
                          neighboring BGP peer.
                        properties:
                          autoDiscovery:
                            description: |-
                              AutoDiscovery allows auto-discovery of peer's IP address.
                              When a peer auto-discovery mechanism is enabled, the peerAddress field
                              can be empty (and will be ignored if set).
                            properties:
                              mode:
                                description: Mode defines the type of BGP peer auto-discovery
                                  mechanism.
                                enum:
                                - Unnumbered
                                type: string
                              unnumbered:
                                description: Unnumbered contains configuration for
                                  the BGP Unnumbered peer auto-discovery mode.
                                properties:
                                  interface:
                                    description: |-
                                      Interface is the name of an interface on the Cilium node to use for BGP unnumbered peering.

                                      The IPv6 link-local address of a neighbor discovered on the specified interface will be used for peering.
                                      Additionally, Router Advertisement messages will be sent out of the configured interface,
                                      so that the neighboring router can learn about Cilium node's link-local IPv6 address as well.
                                    type: string
                                required:
                                - interface
                                type: object
                            required:
                            - mode
                            type: object
                            x-kubernetes-validations:
                            - message: unnumbered field is required for the 'Unnumbered'
                                mode
                              rule: self.mode != 'Unnumbered' || has(self.unnumbered)
                          name:
                            description: Name is the name of the BGP peer. It is a
                              unique identifier for the peer within the BGP instance.
                            maxLength: 255
                            minLength: 1
                            type: string
                          peerASN:
                            default: 0
                            description: |-
                              PeerASN is the ASN of the peer BGP router.
                              Supports extended 32bit ASNs.

                              If peerASN is unspecified or 0, the BGP OPEN message validation of ASN
                              will be disabled and ASN will be determined based on peer's OPEN message.
                            format: int64
                            maximum: 4294967295
                            minimum: 0
                            type: integer
                          peerAddress:
                            description: |-
                              PeerAddress is the IP address of the neighbor.
                              Supports IPv4 and IPv6 addresses.
                            pattern: ((^\s*((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))\s*$)|(^\s*((([0-9A-Fa-f]{1,4}:){7}([0-9A-Fa-f]{1,4}|:))|(([0-9A-Fa-f]{1,4}:){6}(:[0-9A-Fa-f]{1,4}|((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3})|:))|(([0-9A-Fa-f]{1,4}:){5}(((:[0-9A-Fa-f]{1,4}){1,2})|:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3})|:))|(([0-9A-Fa-f]{1,4}:){4}(((:[0-9A-Fa-f]{1,4}){1,3})|((:[0-9A-Fa-f]{1,4})?:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(([0-9A-Fa-f]{1,4}:){3}(((:[0-9A-Fa-f]{1,4}){1,4})|((:[0-9A-Fa-f]{1,4}){0,2}:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(([0-9A-Fa-f]{1,4}:){2}(((:[0-9A-Fa-f]{1,4}){1,5})|((:[0-9A-Fa-f]{1,4}){0,3}:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(([0-9A-Fa-f]{1,4}:){1}(((:[0-9A-Fa-f]{1,4}){1,6})|((:[0-9A-Fa-f]{1,4}){0,4}:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:))|(:(((:[0-9A-Fa-f]{1,4}){1,7})|((:[0-9A-Fa-f]{1,4}){0,5}:((25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)(\.(25[0-5]|2[0-4]\d|1\d\d|[1-9]?\d)){3}))|:)))(%.+)?\s*$))
                            type: string
                          peerConfigRef:
                            description: |-
                              PeerConfigRef is a reference to a peer configuration resource.
                              If not specified, the default BGP configuration is used for this peer.
                            properties:
                              group:
                                default: isovalent.com
                                description: |-
                                  Group is the group of the peer config resource.
                                  If not specified, the default of "isovalent.com" is used.
                                type: string
                              kind:
                                default: IsovalentBGPPeerConfig
                                description: |-
                                  Kind is the kind of the peer config resource.
                                  If not specified, the default of "IsovalentBGPPeerConfig" is used.
                                type: string
                              name:
                                description: |-
                                  Name is the name of the peer config resource.
                                  Name refers to the name of a Kubernetes object (typically a IsovalentBGPPeerConfig).
                                type: string
                            required:
                            - name
                            type: object
                        required:
                        - name
                        type: object
                        x-kubernetes-validations:
                        - message: Either peerAddress or autoDiscovery must be specified
                          rule: has(self.peerAddress) || has(self.autoDiscovery)
                      type: array
                      x-kubernetes-list-map-keys:
                      - name
                      x-kubernetes-list-type: map
                    vrfs:
                      description: VRFs is a list of VRFs for this virtual router
                      items:
                        properties:
                          configRef:
                            description: ConfigRef is a reference to a IsovalentBGPVRFConfig
                              resource.
                            type: string
                          exportRTs:
                            description: ExportRTs is a list of route targets to export
                              routes to.
                            items:
                              type: string
                            type: array
                          importRTs:
                            description: ImportRTs is a list of route targets to import
                              routes from.
                            items:
                              type: string
                            type: array
                          rd:
                            description: RD is the Route Distinguisher of the VRF.
                            type: string
                          vrfRef:
                            description: |-
                              VRFRef is a reference to a IsovalentVRF resource. It should be the same as the name of the
                              IsovalentVRF object to which this BGPVRF is associated.
                            maxLength: 255
                            minLength: 1
                            type: string
                        required:
                        - vrfRef
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - vrfRef
                      x-kubernetes-list-type: map
                  required:
                  - name
                  type: object
                maxItems: 16
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              nodeSelector:
                description: |-
                  NodeSelector selects a group of nodes where this BGP Cluster
                  config applies.
                  If empty / nil this config applies to all nodes.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          enum:
                          - In
                          - NotIn
                          - Exists
                          - DoesNotExist
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      description: MatchLabelsValue represents the value from the
                        MatchLabels {key,value} pair.
                      maxLength: 63
                      pattern: ^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - bgpInstances
            type: object
          status:
            description: Status is a running status of the cluster configuration
            properties:
              conditions:
                description: The current conditions of the IsovalentBGPClusterConfig
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: false
    subresources:
      status: {}
