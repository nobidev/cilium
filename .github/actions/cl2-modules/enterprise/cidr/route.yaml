apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-gateway
spec:
  parentRefs:
  - name: gateway-local-{{.Index}}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /details
    backendRefs:
    - name: details-{{.Index}}
      port: 9080
