name: Enterprise Multi-Network (ci-multi-network)

# Any change in triggers needs to be reflected in the concurrency group.
on:
  workflow_dispatch:
    inputs:
      PR-number:
        description: "Pull request number."
        required: true
      context-ref:
        description: "Context in which the workflow runs. If PR is from a fork, will be the PR target branch (general case). If PR is NOT from a fork, will be the PR branch itself (this allows committers to test changes to workflows directly from PRs)."
        required: true
      SHA:
        description: "SHA under test (head of the PR branch)."
        required: true
      extra-args:
        description: "[JSON object] Arbitrary arguments passed from the trigger comment via regex capture group. Parse with 'fromJson(inputs.extra-args).argName' in workflow."
        required: false
        default: '{}'
  # Run every 6 hours
  schedule:
    - cron: '0 1/6 * * *'

# By specifying the access of one of the scopes, all of those that are not
# specified are set to 'none'.
permissions:
  # Required by the telemetry collection step
  actions: read
  # To be able to access the repository with actions/checkout
  contents: read
  # To allow retrieving information from the PR API
  pull-requests: read
  # To be able to set commit status
  statuses: write

concurrency:
  # Structure:
  # - Workflow name
  # - Event type
  # - A unique identifier depending on event type:
  #   - schedule: SHA
  #   - workflow_dispatch: PR number
  #
  # This structure ensures a unique concurrency group name is generated for each
  # type of testing, such that re-runs will cancel the previous run.
  group: |
    ${{ github.workflow }}
    ${{ github.event_name }}
    ${{
      (github.event_name == 'schedule' && github.sha) ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.PR-number)
    }}
  cancel-in-progress: true

env:
  network-definitions: enterprise/examples/kubernetes/multi-network/isovalentpodnetworks.yaml

jobs:
  commit-status-start:
    name: Commit Status Start
    runs-on: ubuntu-24.04
    steps:
      - name: Set initial commit status
        uses: myrotvorets/set-commit-status-action@3730c0a348a2ace3c110851bed53331bc6406e9f # v2.0.1
        with:
          sha: ${{ inputs.SHA || github.sha }}

  enterprise-multi-network-conformance-test:
    name: Multi-Network Conformance Test
    runs-on: ubuntu-latest-4cores-16gb
    env:
      job_name: 'Setup & Test'
    timeout-minutes: 120
    steps:
      - name: Collect Workflow Telemetry
        uses: catchpoint/workflow-telemetry-action@94c3c3d9567a0205de6da68a76c428ce4e769af1 # v2.0.0
        with:
          comment_on_pr: false

      - name: Checkout context ref (trusted)
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          ref: ${{ inputs.context-ref || github.sha }}
          persist-credentials: false

      - name: Set Environment Variables
        uses: ./.github/actions/set-env-variables

      - name: Set up job variables
        id: vars
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            SHA="${{ inputs.SHA }}"
            CONTEXT_REF="${{ inputs.context-ref }}"
          else
            SHA="${{ github.sha }}"
            CONTEXT_REF="${{ github.sha }}"
          fi

          echo sha=${SHA} >> $GITHUB_OUTPUT
          echo context-ref=${CONTEXT_REF} >> $GITHUB_OUTPUT

          # Notes:
          #  - Multi-network only supports direct routing, thus we disable
          #    tunnel mode and enable auto-direct-routes.
          #  - Multi-network only supports endpoint routes, thus we disable
          #    the local-node-route.
          #  - iptables-based masquerading does not support multiple non-masquerade
          #    CIDRs. Thus, we enable BPF masquerading where we can add multiple
          #    non-masquerade CIDRs.
          CILIUM_INSTALL_DEFAULTS="--chart-directory=install/kubernetes/cilium \
            --helm-set=debug.enabled=true \
            --helm-set=image.repository=quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/cilium-ci \
            --helm-set=image.useDigest=false \
            --helm-set=image.tag=${SHA} \
            --helm-set=operator.image.repository=quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/operator \
            --helm-set=operator.image.suffix=-ci \
            --helm-set=operator.image.tag=${SHA} \
            --helm-set=operator.image.useDigest=false \
            --helm-set=hubble.relay.enabled=true \
            --helm-set=hubble.relay.image.repository=quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/hubble-relay-ci \
            --helm-set=hubble.relay.image.tag=${SHA} \
            --helm-set=hubble.relay.image.useDigest=false \
            --helm-set=routingMode=native \
            --helm-set=endpointRoutes.enabled=true \
            --helm-set-string=extraConfig.enable-local-node-route=false \
            --helm-set=kubeProxyReplacement=true \
            --helm-set=bpf.masquerade=true \
            --helm-set=devices='{eth+}' \
            --helm-set=ipv4NativeRoutingCIDR=10.0.0.0/8 \
            --helm-set=ipMasqAgent.enabled=true \
            --helm-set=ipMasqAgent.config.nonMasqueradeCIDRs='{192.168.0.0/16}' \
            --helm-set=ipam.mode=multi-pool \
            --helm-set=ipam.operator.autoCreateCiliumPodIPPools.default.ipv4.cidrs='{10.10.0.0/16}' \
            --helm-set=ipam.operator.autoCreateCiliumPodIPPools.default.ipv4.maskSize=24 \
            --helm-set=ipam.operator.autoCreateCiliumPodIPPools.secondary-pool.ipv4.cidrs='{192.168.16.0/20}' \
            --helm-set=ipam.operator.autoCreateCiliumPodIPPools.secondary-pool.ipv4.maskSize=27 \
            --helm-set=enterprise.multiNetwork.enabled=true \
            --helm-set=enterprise.featureGate.strict=true \
            --helm-set='enterprise.featureGate.approved={IPAMMultiPool,MultiNetwork}' \
            --helm-set=enterprise.multiNetwork.autoCreateDefaultPodNetwork=true"

          CONNECTIVITY_TEST_DEFAULTS="--flow-validation=disabled --hubble=false --collect-sysdump-on-failure \
            --log-code-owners --code-owners=${CILIUM_CLI_CODE_OWNERS_PATHS} \
            --exclude-code-owners=${CILIUM_CLI_EXCLUDE_OWNERS} \
            --deployment-pod-annotations='{ \
              \"*\":{\"network.v1alpha1.isovalent.com/pod-networks\":\"default,secondary-network\"}, \
              \"echo-other-node\":{\"network.v1alpha1.isovalent.com/pod-networks\":\"secondary-network\"}, \
              \"client2\":{\"network.v1alpha1.isovalent.com/pod-networks\":\"secondary-network\"} \
            }'"

          echo cilium_install_defaults=${CILIUM_INSTALL_DEFAULTS} >> $GITHUB_OUTPUT
          echo connectivity_test_defaults=${CONNECTIVITY_TEST_DEFAULTS} >> $GITHUB_OUTPUT

      # Warning: since this is a privileged workflow, subsequent workflow job
      # steps must take care not to execute untrusted code.
      - name: Checkout pull request branch (NOT TRUSTED)
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          ref: ${{ steps.vars.outputs.sha }}
          persist-credentials: false

      - name: Install Cilium CLI
        uses: cilium/cilium-cli@db1773e48b18184dd2843eb5383cec4c70b228c5 # v0.18.8
        with:
          skip-build: ${{ env.CILIUM_CLI_SKIP_BUILD }}
          image-repo: ${{ env.CILIUM_CLI_IMAGE_REPO }}
          image-tag: ${{ steps.vars.outputs.sha }}
          repository: ${{ env.CILIUM_CLI_RELEASE_REPO }}
          release-version: ${{ env.CILIUM_CLI_VERSION }}
          binary-dir: ./

      - name: Provision LVH VMs
        uses: cilium/little-vm-helper@ad43ff511dd5365a0011ae9f864ec959c36daebf # v0.0.28
        with:
          test-name: enterprise-multi-network
          # renovate: datasource=docker depName=quay.io/lvh-images/kind
          image-version: '6.1-20240201.165956'
          host-mount: ./
          cpu: 4
          install-dependencies: 'true'
          cmd: |
            git config --global --add safe.directory /host

      - name: Wait for images to be available
        timeout-minutes: 30
        shell: bash
        run: |
          for image in cilium-ci operator-generic-ci hubble-relay-ci; do
            until docker manifest inspect quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/$image:${{ steps.vars.outputs.sha }} &> /dev/null; do sleep 45s; done
          done

      - name: Run connectivity tests
        uses: cilium/little-vm-helper@ad43ff511dd5365a0011ae9f864ec959c36daebf # v0.0.28
        with:
          provision: 'false'
          cmd: |
            set -eux

            cd /host/

            # create kind cluster with secondary network named "secondary-network"
            IPFAMILY=ipv4 IMAGE=${{ env.KIND_K8S_IMAGE }} ./contrib/scripts/kind.sh --secondary-network "1" "2"

            ./cilium install ${{ steps.vars.outputs.cilium_install_defaults }}
            ./cilium status --wait

            # Create 'secondary-network' IsovalentPodNetwork
            kubectl apply -f ${{ env.network-definitions }}

            kubectl get pods --all-namespaces -o wide
            kubectl get cep --all-namespaces -o wide
            kubectl -n kube-system exec daemonset/cilium -- cilium status
            kubectl get isovalentpodnetworks -o yaml

            # Verify that both IsovalentPodNetworks got created
            kubectl get isovalentpodnetworks -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' | grep "^default$"
            kubectl get isovalentpodnetworks -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' | grep "^secondary-network$"

            mkdir -p cilium-junits

            ./cilium connectivity test ${{ steps.vars.outputs.connectivity_test_defaults }} \
              --test-concurrency=5 \
              --junit-file "cilium-junits/${{ env.job_name }} - 1.xml" \
              --junit-property github_job_step="Run connectivity tests" \
              --test '!pod-to-pod-encryption,!node-to-node-encryption'

            kubectl get pods --all-namespaces -o wide
            kubectl get cep --all-namespaces -o wide

            # Check that endpoints got created for secondary network
            CLIENT_CEP=\$(kubectl -n cilium-test-1 get cep -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' | grep "^client-.*-.*-cil1")
            SAME_NODE_CEP=\$(kubectl -n cilium-test-1 get cep -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' | grep "^echo-same-node-.*-.*-cil1")

            # Check that CNI generated labels got added to identity labels of the CEPs
            kubectl -n cilium-test-1 get cep \$CLIENT_CEP -o jsonpath='{.status.identity.labels}' | grep "cni:com.isovalent.v1alpha1.network.attachment=secondary-network"
            kubectl -n cilium-test-1 get cep \$SAME_NODE_CEP -o jsonpath='{.status.identity.labels}' | grep "cni:com.isovalent.v1alpha1.network.attachment=secondary-network"

            CLIENT_POD=\$(kubectl -n cilium-test-1 get pod -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' | grep "^client-.*-.*")
            SAME_NODE_POD=\$(kubectl -n cilium-test-1 get pod -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' | grep "^echo-same-node-.*-.*")

            # Check that multi-homed pods got routes set up for secondary network
            kubectl -n cilium-test-1 exec -ti \$CLIENT_POD -- ip route | grep "^192\.168\.0\.0/16 via 192\.168\.0\.1 dev cil1"
            kubectl -n cilium-test-1 exec -ti \$SAME_NODE_POD -- ip route | grep "^192\.168\.0\.0/16 via 192\.168\.0\.1 dev cil1"

            ./cilium features status -o json --output-file="features-tested-enterprise-multi-network.json"

            ./contrib/scripts/kind-down.sh

      - name: Fetch artifacts
        if: ${{ !success() }}
        uses: cilium/little-vm-helper@ad43ff511dd5365a0011ae9f864ec959c36daebf # v0.0.28
        with:
          provision: 'false'
          cmd: |
            cd /host
            kubectl get pods --all-namespaces -o wide
            ./cilium status
            mkdir -p cilium-sysdumps
            ./cilium sysdump

      - name: Run common post steps
        if: ${{ always() }}
        uses: ./.github/actions/post-logic
        with:
          artifacts_suffix: "enterprise-multi-network"
          job_status: "${{ job.status }}"
          capture_features_tested: false
          capture_sysdump: false

  merge-upload-and-status:
    name: Merge Upload and Status
    if: ${{ always() }}
    needs: enterprise-multi-network-conformance-test
    uses: ./.github/workflows/common-post-jobs.yaml
    secrets: inherit
    with:
      context-ref: ${{ inputs.context-ref || github.sha }}
      sha: ${{ inputs.SHA || github.sha }}
      success: ${{ needs.enterprise-multi-network-conformance-test.result == 'success' }}
