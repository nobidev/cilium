name: Greetings

on:
  pull_request_target:
    types: [opened]
  issues:
    types: [opened]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  greet:
    uses: cilium/cilium/.github/workflows/reusable-greetings.yml@main
    secrets: inherit
