name: OLM Build CI

on:
  pull_request:
    branches:
      - main-ce
      # TODO: cover release branches later on
    paths:
      - enterprise/olm/**
  push:
    branches:
      - main-ce
      # TODO: cover release branches later on
    paths:
      - enterprise/olm/**

permissions:
  # To be able to access the repository with `actions/checkout`
  contents: read

jobs:
  build-and-push-prs:
    timeout-minutes: 15
    name: Build and Push Images
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout the repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
          sparse-checkout: |
            .github/actions
            enterprise/olm
            install/kubernetes

      - name: Set Environment Variables
        uses: ./.github/actions/set-env-variables

      - name: Setup go
        uses: actions/setup-go@0aaccfd150d50ccaeb58ebd88d36e91967a5f35b # v5.4.0
        with:
          cache: false
          # renovate: datasource=golang-version depName=go
          go-version: 1.24.1

      - name: Login to quay.io
        uses: docker/login-action@74a5d142397b4f367a81961eba4e8cd7edddf772 # v3.4.0
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_USERNAME_CI }}
          password: ${{ secrets.QUAY_PASSWORD_CI }}

      - name: Getting image tag
        id: tag
        run: |
          if [ "${{ github.event.pull_request.head.sha }}" != "" ]; then
            tag=${{ github.event.pull_request.head.sha }}
          else
            tag=${{ github.sha }}
          fi
          echo tag=${tag} >> $GITHUB_OUTPUT

      - name: Build and Push OLM container image
        # TODO: Sign container image and generate SBOM
        # TODO: Multi-arch builds
        env:
          IMG: "quay.io/${{ env.QUAY_ORGANIZATION_DEV }}/clife-ci:${{ steps.tag.outputs.tag }}"
        run: |
          # Sync manifests first
          rm -rf enterprise/olm/manifests
          cp -a ./install/kubernetes/cilium/. enterprise/olm/manifests
          cd enterprise/olm
          make docker-build docker-push

