allowed-teams:
  - isovalent

triggers:
  /default:
    workflows:
      - enterprise-kind.yaml
  # This trigger needs to be kept in sync with enterprise-ariane-scheduled.yaml workflow
  /test\s*:
    workflows:
    - enterprise-clustermesh-mixed-routing.yaml
    - enterprise-clustermesh-overlapping-podcidr.yaml
    - enterprise-fqdn-ha.yaml
    - enterprise-fqdn-ha-upgrade.yaml
    - enterprise-multi-network.yaml
    - enterprise-srv6-datapath.yaml
    - enterprise-multicast.yaml
    - enterprise-lb.yaml
    - enterprise-encryption-policy.yaml
    - enterprise-ingress-policy.yaml
    - enterprise-standalone-egress-gateway.yaml
    depends-on:
    - /build-images
  /backport (.+):
    workflows:
    - enterprise-auto-backport.yaml
  /ci-clustermesh:
    workflows:
    - enterprise-clustermesh-mixed-routing.yaml
    - enterprise-clustermesh-overlapping-podcidr.yaml
    depends-on:
    - /build-images
  # `(.+)` is regex capture group that is used as `extra-args` inputs parameter in the CuTE workflow
  # For example: `/cute bloomberg` or `/cute form3/MAKS`
  /cute (.+):
    workflows:
    - enterprise-cute.yaml
  /ci-multi-network:
    workflows:
    - enterprise-multi-network.yaml
    depends-on:
    - /build-images
  /ci-fqdn-ha:
    workflows:
    - enterprise-fqdn-ha.yaml
    depends-on:
    - /build-images
  /ci-fqdn-ha-upgrade:
    workflows:
    - enterprise-fqdn-ha-upgrade.yaml
    depends-on:
    - /build-images
  /ci-srv6-datapath:
    workflows:
    - enterprise-srv6-datapath.yaml
    depends-on:
    - /build-images
  /ci-multicast:
    workflows:
    - enterprise-multicast.yaml
    depends-on:
    - /build-images
  /fqdn-ha-perf:
    workflows:
    - enterprise-fqdn-ha-perf.yaml
    depends-on:
    - /build-images
  /ci-lb:
    workflows:
    - enterprise-lb.yaml
    depends-on:
    - /build-images
  /ci-lb-vsphere:
    workflows:
    - enterprise-lb-vsphere.yaml
    depends-on:
    - /build-images
  /scale-encryption-policy:
    workflows:
    - enterprise-scale-encryption-policy.yaml
    depends-on:
    - /build-images
  /ci-encryption-policy:
    workflows:
    - enterprise-encryption-policy.yaml
    depends-on:
    - /build-images
  # `(?:\s*)({.*})?` is regex capture group that is used as `extra-args` inputs parameter in the enterprise conformance OpenShift workflow
  # For example: `/ci-openshift {"kubevirt":"true", "ocp_version":"4.17.14"}`
  /ci-openshift(?:\s*)({.*})?:
    workflows:
    - enterprise-conformance-openshift.yaml
  # `(?:\s*)({.*})?` is regex capture group that is used as `extra-args` inputs parameter in the enterprise conformance OpenShift workflow
  # For example: `/ci-ilb-openshift {"ocp_version":"4.17.14"}`
  /ci-ilb-openshift(?:\s*)({.*})?:
    workflows:
    - enterprise-conformance-ilb-openshift.yaml
  /ci-panda-scale(\s+(?:nodes=[0-9]+)?(?:\s*version=[-+.0-9a-z]+)?(?:\s*sha=[a-f0-9]+)?)?:
    workflows:
    - enterprise-panda-scale.yaml
  /ci-cidr-scale(\s+(?:nodes=[0-9]+)?(?:\s*version=[-+.0-9a-z]+)?(?:\s*sha=[a-f0-9]+)?)?:
    workflows:
    - enterprise-cidr-scale.yaml
  /ci-ingress-policy:
    workflows:
    - enterprise-ingress-policy.yaml
    depends-on:
    - /build-images
  /ci-standalone-egress-gateway:
    workflows:
    - enterprise-standalone-egress-gateway.yaml
    depends-on:
    - /build-images
  /ci-egress-gateway-ha-eks:
    workflows:
    - enterprise-egress-gateway-ha-eks.yaml
    depends-on:
    - /build-images
  /ci-egress-gateway-ha-aks:
    workflows:
    - enterprise-egress-gateway-ha-aks.yaml
    depends-on:
    - /build-images
  /build-images: #this trigger is not meant to be used in comments, as oss build workflows are not triggered by Ariane. Having this trigger allows us to have others depend on oss images workflow though.
    workflows:
    - enterprise-build-images-ci.yaml

# This trigger needs to be kept in sync with enterprise-ariane-scheduled.yaml workflow
schedule:
  nightly:
    workflows:
    - conformance-gke.yaml

workflows:
  enterprise-clustermesh-mixed-routing.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-clustermesh-overlapping-podcidr.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-fqdn-ha.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-fqdn-ha-upgrade.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-multi-network.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-srv6-datapath.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-multicast.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-lb.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-encryption-policy.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-lb-vsphere.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-conformance-openshift.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-ingress-policy.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)
  enterprise-standalone-egress-gateway.yaml:
    paths-ignore-regex: ((bpf/complexity-tests|bpf/tests|test|Documentation|hubble|.github/actions/cl2-modules)/|(.github/renovate\.json5|README.rst|CODEOWNERS|stable.txt|.*\.md|.*_test\.go)$)

feedback:
  verbose: true
  workflows-report: true

rerun:
  max-retries: 5
