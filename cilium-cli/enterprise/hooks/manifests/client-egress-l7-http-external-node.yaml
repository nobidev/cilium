---
# All clients are allowed to contact
# echo-external-node.cilium-test-1.svc.cluster.local/client-ip
# on port http-8090.
# The toFQDNs section relies on DNS introspection being performed by
# the client-egress-only-dns policy.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: client-egress-l7-http-external-node
spec:
  description: "Allow GET echo-external-node.cilium-test-1.svc.cluster.local:8090/client-ip"
  endpointSelector:
    matchLabels:
      any:kind: client
  egress:
  - toFQDNs:
    - matchName: "echo-external-node.cilium-test-1.svc.cluster.local"
    toPorts:
    - ports:
      - port: "8090"
        protocol: TCP
      rules:
        http:
        - method: GET
          path: /client-ip
